[pytest]
# Aufruf aus Code/:  .\venv\Scripts\python.exe -m pytest
# (tests/conftest.py haengt Code/ an sys.path und biegt vorher alles weg, was
# echte Nutzerdaten waeren: LOCALAPPDATA, config.json, DPAPI-Pepper.)
testpaths = tests
addopts = -q
