# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Writing style (mandatory)

The user does not like dashes (Gedankenstriche). Do NOT use the em-dash (Unicode U+2014) or the en-dash (Unicode U+2013) anywhere: not in code, comments, docstrings, UI strings, commit messages, documentation, or any file in this repo, and not in chat replies. Use commas, colons, parentheses, or separate sentences instead. For numeric ranges use "bis"/"to" or a plain ASCII hyphen (e.g. "0-11"). Plain ASCII hyphens in compound words and code identifiers are fine; only the two long dashes are forbidden.

## The plan is the highest authority (mandatory)

`Planung/Bauplan - NoaToDo.md` is **the** binding document for this project. It outranks
this file, the code, and any assumption: when they disagree, the Bauplan wins and the other
side is the bug. `Planung/Plananalyse - Schwachstellen und Angriffsvektoren.md` is its
standing audit (findings W*/S*/U*/V*; a finding is resolved only when the Bauplan says so).

**Read before you build:** open the Bauplan section for the phase you are touching, plus its
security gates (B.9), *before* writing code. Never implement from memory or from this file
alone.

**Check the plan after every finished task, every time.** When you complete anything
(a feature, a fix, a decision), go back into the Bauplan and ask:
1. Is there a checkbox, a phase line, or a gate row that must now be ticked off or moved to
   "erledigt" (with the date)?
2. Did this change make some part of the plan wrong, stale, or contradictory? Then fix that
   part in the same change. The plan must never claim something the code does not do (that
   is exactly how Plananalyse finding S7 happened: the plan claimed a per-task hover trash
   button that never existed).
3. Does the change touch a place the plan calls a single source of truth (B.2 bridge API +
   error codes, B.5 shortcuts, B.9 gates, the Entscheidungsregister in Anhang 1; since the
   2026-07 Struktur-Umbau a new decision goes into its Teil-B contract plus a register row,
   never into a new Nachtrag block)? Then update that place too,
   in the same change, and mirror it here in CLAUDE.md.

A change that leaves the plan out of sync is not finished.

## Commit as you go (mandatory)

The user wants working state saved in git, not held in the working tree. An agent working in
this repo therefore commits **on its own initiative**, without being asked each time:

1. **After every self-contained piece of work.** A feature that works, a bug that is fixed, a
   refactoring that is complete: commit it. "Self-contained" means the app still starts and
   the piece does what it claims; it does not mean the whole phase is finished.
2. **At every important point**, even mid-feature: before a risky or wide-reaching change,
   before deleting or rewriting something large, after a working intermediate state that
   would hurt to lose, and at the end of a work session.
3. **Including the plan.** Per the section above, the Bauplan/CLAUDE.md updates belong in the
   **same** commit as the code they describe, never in a separate later one.

Commit messages follow the existing style (`Phase 8: <what changed>`, German, no em/en dash
per the writing-style rule). Use `git commit -F <message file>` instead of `-m` with a
here-string: PowerShell 5.1 breaks on embedded quotes. Do not `push`, do not force-push, and
do not switch or create branches on your own; committing is the automatic part, everything
else stays a decision the user makes.

## Project status

**Phase 9 (build/packaging) is IN PROGRESS on branch `phase-9`, started 2026-08-10.** The delivery decisions are settled and binding (Bauplan N11.29, register row in Anhang 1): the release is built with **PyInstaller as a one-file `NoaToDo.exe`** (not Nuitka; G27's Nuitka preference is deliberately deferred and named honestly, its mandatory part "no Python source, no docstrings, no asserts" is met via `--optimize 2`), **without code signing** (no certificate: G27 stays open there, and the SmartScreen warning plus the missing tamper detection on the binary are documented, never talked away), and the **Evergreen WebView2 runtime is assumed present** on the target machine (nothing bundled; the mandatory part is the understandable message in the app design instead of a white window). First shipped version is **1.0.0**. **`Code/buildinfo.py` is the one source for everything that differs between "started from the source tree" and "frozen exe":** `VERSION`/`SOURCE_URL`, the optional build stamp (`_buildstamp.py`, generated by the build script, carrying build date, commit and the G27 frontend hash manifest), `is_frozen()`/`is_release()`, `debug_enabled()` (**hard `False` in the frozen build, G34 a**, so `NOATODO_DEBUG` cannot open DevTools in a release), and the path resolution `bundle_dir()`/`resource_path()`/`frontend_dir()`/`index_html()`/`icon_path()` (one-file unpacks the frontend into `sys._MEIPASS` per start, so `__file__`-relative paths are wrong there) plus `app_dir()` (folder the app lives in: the exe's folder when frozen, used only for the G33 legacy `data/tasks.db`, never for vault/config/profile). `main.py`, `lockwindow.py` and `api.py._cleanup_dev_legacy_db()` all go through it; do not add a second `__file__`-relative asset path or a second debug switch.

**Release hardening of the WebView (G34 a/c) and the WebView2 runtime check (N11.29 c), built 2026-08-10.** `main._harden_release_webview(window)` runs from `on_start` and, **only when `buildinfo.is_release()`** (i.e. frozen), sets three CoreWebView2 settings to `False`: `AreDevToolsEnabled` (belt and suspenders next to the hard `debug=False`, because a DevTools console is full `pywebview.api.*` access including `killswitch()`), `AreBrowserAcceleratorKeysEnabled` (kills `Ctrl+P` = "print to PDF" as a full plaintext export past G21, plus the other browser keys; the B.5 shortcuts run through the app's own JS handler and are untouched) and `AreDefaultContextMenusEnabled`. `CoreWebView2` often does not exist yet when the handler attaches, so it falls back to `CoreWebView2InitializationCompleted`; each property is set in its own `try` so an older runtime that lacks one cannot take the others down (G27 guideline: hardening must never break the app). The dev start keeps DevTools and the context menu. **Runtime check:** `main._webview2_ready()` does **not** query the registry, it reads PyWebView's own `webview.platforms.winforms.renderer` and demands `edgechromium`; without the runtime PyWebView silently falls back to MSHTML (Internet Explorer), which opens a window showing a broken picture instead of an honest message. If the check fails, `_show_missing_webview2()` shows the themed hint window (`wintheme.show_message`, no WebView needed) naming the missing component and `microsoft.com/edge/webview2`, then the app exits cleanly. It cannot be a false negative (what PyWebView does not see as WebView2 it does not use), and if the module cannot be asked at all the app starts anyway (never block without need). `wintheme.show_message` now grows its height with the number of text lines, so longer messages are not clipped; short ones (G19) keep the old size.

**Frontend integrity (G27/A5), built 2026-08-10: `Code/integrity.py`.** `build_manifest(dir)` hashes every tracked frontend file (`.html/.js/.css/.woff2/.ico/.svg`, relative paths with `/`, sorted) with SHA-256; the build script writes that dict into `_buildstamp.py`, from where `buildinfo.FRONTEND_MANIFEST` picks it up. `check()` re-hashes each entry at startup and returns the offending names (missing counts as offending); `main()` calls it right after the WebView2 check and, on any mismatch, shows the themed message from `integrity.message(bad)` and **exits without starting** (there is deliberately no "continue anyway" button, because manipulated `app.js` has full bridge access and can read the passphrase out of the lock screen). Without an embedded manifest (normal dev start) `check()` is a no-op, otherwise every frontend edit would cry wolf. NoaToDo satisfies both G27 paths on purpose: one-file already carries the frontend **inside** the binary (nothing lies next to the exe to swap), and the hash check runs on top of it. Honest scope (B.10/G22): this raises the bar against silent K4 persistence, it is not full K4 protection, and with no code signing in this version (N11.29 b) a swapped **binary** is not caught at all.

**Old redirected paths + version in the status modal, built 2026-08-10.** `main._cleanup_redirected_legacy()` is the one-time first-start step G14/N11.15.5 (d) asks for: it runs **only when frozen** (in the source tree the redirected path *is* the live working path, wiping it there would destroy the running dev setup), walks `%LOCALAPPDATA%\Packages\PythonSoftwareFoundation.Python.*\LocalCache\Local\NoaToDo\`, removes the stale `webview` profile folder and the stale `config.json` there, and **never touches a `tasks.db.enc`** (clean-up yes, migration never: the old config is deleted, not read). A marker file `%LOCALAPPDATA%\NoaToDo\redirect-cleanup.done` keeps it to one run. **V10:** `get_status()` now carries `app` from `buildinfo.build_info()`, and the status modal renders three more rows from it: "Version" (`1.0.0 · <build date>` or `· source build`, tagged `release`/`dev`), "Code signature" (`not signed` for this version, `signed` only when the build step really signed, `not applicable` in the source tree, so the unsigned build never reads as signed, G22) and "Updates" (`manual · <source>`, because there is deliberately no auto-update and no update check over the network).

**Phase 8 is DONE (2026-07-21): the security core is real.** `backend/security.py` is no longer a stub. Built and verified: Argon2id key derivation (params N11.4.3: 256 MiB, t=3, p=4) with the DPAPI pepper bound in via `HKDF-Extract(salt=pepper, ikm=passphrase)` (V2a) then HKDF-SHA256 domain separation to `aes_key`/`chacha_key` (G15/G18); the dual cascade ChaCha20-Poly1305(SQLCipher-AES-256) with the `tasks.db.enc` header format (G16/V1: header as AEAD associated_data, param acceptance range, probe-decrypt before `.bak` rotation, free-space check); the `Vault` session (N11.9 fallback: a SQLCipher-**encrypted** working file under `%LOCALAPPDATA%\NoaToDo\work`, never plaintext, snapshot via `VACUUM INTO`, because `sqlcipher3` exposes no `:memory:` `serialize`, proven by the U3 spike); the persisted unlock rate-limit ladder (B.8.4/N11.4.1, persist-before-verify); the debounced write-back (G17, ~3 s / 30 s cap); the fail-safe auto-lock timer (B.8.3/N11.4.2); and the single `teardown(reason)` sequence (G35/N11.11) that every exit runs through. `backend/config.py` holds the unencrypted `config.json` (B.11). The lock screen is a **native WinForms window** (`Code/lockwindow.py`) with no WebView: the U3/N11.8.3 spike proved PyWebView cannot run two WebView2 profiles in one process, so the native fallback is binding and there is **no `LOCK_PROFILE_DIR`** (Register N11.18). `main.py` runs a boot loop that alternates the native lock window (locked/vault_error) and the WebView main window (onboarding/unlocked), wiping `PROFILE_DIR` (G14) after each window teardown. The frontend has the three-step onboarding (N11.13), `get_boot_state()` as the first boot call, `activity_ping` throttled on DOM input, the auto-lock setting, and a passphrase-change modal. **Welcome screen (N11.20, 2026-07-25; narrowed by N11.22, 2026-08-08):** after a successful `create_vault()` and on every **normal start** (but **no longer after unlocking** a running session, see N11.22 below) `renderWelcome()` in `app.js` lays a full-screen welcome over the already rendered app: the logo mark (inline SVG ring + N, no image asset, so no CSP exception) glimmers dimmed in the background (ring draws itself, a light sweep runs across it, plus a soft accent glow), "Welcome to NoaToDo" flies in character by character (the honest subline "Your vault is ready. Everything stays on this PC." appears **only** in the post-onboarding variant; on a normal start `state.welcome.greetingOnly` drops it, so the greeting alone sits exactly in the middle of the mark), and after ~2.9 s (normal start) resp. ~4.8 s (right after `create_vault()`, so the subline stays readable) the whole screen fades out onto the app in ~0.5 s (`WELCOME_HOLD_MS`/`WELCOME_HOLD_VAULT_MS`/`WELCOME_FADE_MS`; the fade runs in-place via a `leaving` class, never through `render()`, or the intro animations would restart). Any click or key press skips it and does nothing else; `clearWorkspace()` drops it instantly (`cancelWelcome()`) so it can never hang over the lock cover. It holds nothing up: the auto-lock timer keeps running and no bridge call is involved. **The sweep must stay perfectly smooth (2026-08-08, user report "it sometimes freezes"), and two things caused that, both now fixed and both binding (Bauplan N11.20, last bullet):** (a) its old `mask-position` end frames left the band **half over** the mark, so during the quiet part of the 3.4 s cycle a dim streak just stood there; the travel now ends fully outside the mark on both sides. (b) `mask-position` is not a compositor property, so every frame repainted the whole 500px mark and stuttered under load; the mask now sits still and two **counter-running** `transform` animations do the work (`.wl-sheen` carries the mask and travels, `.wl-sheen-in` holds the mark visually still by moving exactly the opposite way, `wl-sheen-sweep`/`wl-sheen-hold` in `style.css`, same duration/easing/delay so they can never drift). Do not go back to animating the mask. For the same reason the WiFi poll no longer starts inside the welcome (`enterUnlockedApp` skips it while `state.welcome` is set, `finishWelcome` starts it after the fade): its first call fires immediately and runs `netsh` in the backend, which showed up as a hitch in the intro. **N11.22 (2026-08-08):** unlocking a running session no longer shows it, `get_boot_state()` now carries `resumed` (`true` = the session was locked in between, set by main.py's boot loop) and `boot()` only sets `state.welcome` when `resumed` is false; the feedback on unlock is the opening lock in the native lock window (see the lock-window paragraph). The counterpart is N11.26 (2026-08-08): on a normal start the lock deliberately does **not** open, so the welcome screen is the only greeting there. `DEV_AES_KEY` is **gone** (G9). Gates done in Phase 8: G6, G7, G8, G9, G13, G14, G15, G16, G17, G18, G19-rest (Global\ SID mutex, V3), G25, G28, G31, G32, G33, G35 (plus G30 read first). Encryption is real, so `get_status()`/the status modal now report the real active state (both layers, Argon2 params, pepper present, real BitLocker status via WMI or honest "unknown"). Two crypto/API test tools live in `Code/tools/` (`verify_crypto.py` = the standalone G28 proof + roundtrip + hardening; `spike_u3_lockwindow.py` = the U3 spike). **Separate N11 features (both built):** N11.5 the real Windows airplane-mode toggle, **built 2026-07-21**, see the airplane-mode paragraph below; and N11.6 theme-follows-Windows + the sound-setting UI, **built 2026-08-08**, see the theme paragraph below. The last two setup-UI bits were finished on 2026-08-08, so **every Phase-8 gate is now complete**: the BitLocker recommendation (G31a) and the acknowledged SSD-residue one-time notice (G33), see the onboarding paragraph below. Next is Phase 9 (build/packaging).

**BitLocker recommendation + SSD-residue notice are DONE (G31a/G33, 2026-08-08), which closes the last two Phase-8 gate remainders.** `choose_vault_dir()` now also returns `bitlocker: {state: 'protected'|'off'|'unknown', drive}` (reusing the existing `_bitlocker_status()` WMI query that already fed the status modal), and onboarding step 1 renders it via `renderBitlockerNote()` in `app.js` as soon as a location is chosen. It has **three** texts on purpose, because a measured "off" and an unreadable status must not claim the same thing (G22): `protected` confirms the channels the app cannot reach by itself are covered (pagefile, hibernation file, crash dumps), `off` strongly recommends turning it on and names those channels, and `unknown` says outright that NoaToDo does not know (the query normally needs admin rights, which is the **common** case: on a non-admin machine WMI, `Get-BitLockerVolume` and `manage-bde` all return access denied) and asks the user to check in Windows settings. Recommendation, never a block. For G33, `_cleanup_dev_legacy_db()` now returns whether it actually found and removed something, `create_vault()` passes that through as `legacy_removed`, and only then does onboarding show a fourth step: a **must-acknowledge** one-time notice ("I have read this") before the welcome fade, naming both residual channels (wear leveling makes the overwrite best effort, not a guarantee; old export files are the user's own files that the app deliberately neither looks for nor deletes) and pointing at full-disk encryption as the only complete answer. No find means no notice (a notice on spec would be a claim into the blue), and it can only appear once because the old DB is gone afterwards. The step lives inside the onboarding boot state, so there is no `Esc` past it.

**Theme follows Windows + sound toggle are DONE (N11.6, 2026-08-08).** The `dark` bool setting is gone; `theme` = `auto|light|dark` (default `auto`) is the only theme key, and `toolbar` is gone too. `backend/ostheme.py` (new) reads and watches the Windows state: `read_os_dark()` returns `True|False|None` from `HKCU\Software\Microsoft\Windows\CurrentVersion\Themes\Personalize\AppsUseLightTheme` (`None` = honestly unreadable, the caller then keeps its current theme instead of guessing a switch), and `ThemeWatcher` runs a daemon thread whose single `WaitForMultipleObjects` combines both required paths: `RegNotifyChangeKeyValue` on that key (event-based, the main path, re-registered before every wait because the Win32 contract fires only once) and the mandated **60 s counter-check** as its timeout (`RECHECK_MS`). Deliberately no window class and no `WM_SETTINGCHANGE` hook, so it needs no message loop and never touches the WinForms UI thread. `Api._effective_dark()` is the **one** resolution (fixed theme wins; `auto` asks the OS; unreadable stays dark) and is used by both consumers: the frontend via `effectiveTheme()` in `app.js` and the **native titlebar** via `main.py._current_dark()`, which previously read a nonexistent `api.db` and therefore always returned dark. The OS theme rides along in `get_state()` as `system_theme: 'dark'|'light'|'unknown'` so `theme=auto` is correct on the **first** render (no catch-up, B.6) without an extra bridge call; live changes arrive event-driven as `window.noa.onSystemTheme(dark)`, which only sets `data-theme` via `applyChrome()` (never `render()`, same reasoning as `onNetChange`). `_theme_setting`/`_effective_dark`/`_ensure_theme_mirror`/`_on_os_theme_change` all carry the `_` prefix on purpose: a public method would become a JS bridge method and bypass the G13 allowlist. `Ctrl+J` follows U16 in one line (`setSetting('theme', effectiveTheme() === 'dark' ? 'light' : 'dark')`): from `auto` that is the override to the opposite of what is shown, from a fixed theme the other fixed theme, and it never returns to `auto`; only the Appearance segment (`Auto|Light|Dark`) does. The settings modal gained that segment plus a **Sound** section (`sound`, default on) and `playDoneSound()` returns early when it is off. **Migration (N11.23):** `db._migrate_settings()` runs on every open and gives any vault without a `theme` row a one-time `theme = auto`, deleting the `dark` and `toolbar` rows; an existing `dark=true` is deliberately **not** mapped to fixed dark, because that would keep an existing user out of the very automatic state the change introduces. Both old keys are out of the G20 whitelist, so writing them returns `invalid`.

**Native windows carry the app design (N11.21, 2026-07-25).** No window NoaToDo opens may look like plain Windows; the binding contract is Bauplan B.3 ("Das Design gilt auch nativ") plus the N4 addition in B.4. `Code/wintheme.py` is the single implementation of the native look: the dark design tokens copied 1:1 from `style.css`, the DWM titlebar (`DWMWA_USE_IMMERSIVE_DARK_MODE` + `CAPTION_COLOR` = `--surface` + `TEXT_COLOR`, so no white caption anywhere, followed by a `SetWindowPos(FRAMECHANGED)` so the color lands immediately), the app backdrop (base tone + 28px grid, painted with the caller's absolute offset so the grid runs on across control borders), and self-drawn (GDI+, antialiased) pill controls: `PillButton` (primary/ghost/danger/icon with hover/pressed/disabled states), `PillInput` (rounded panel + borderless TextBox, `EM_SETCUEBANNER` placeholder, accent border on focus), `TextLink`, `AppLabel`, plus `show_message()`, the themed hint window that replaced the white `MessageBox` of the single-instance notice (G19). `main.py._apply_titlebar_theme` now delegates there (the old duplicate constants are gone). **The lock window (`lockwindow.py`) was rebuilt on it (same behaviour, new looks):** it starts **maximized** like the main window, so locking/unlocking no longer pops a small grey dialog; it shows the designed lock screen (grid backdrop, 184px accent-wash ring with the `Icons.Lock` glyph redrawn in GDI+ on the same 24-unit grid and 1.7 stroke, pill password field (no plaintext reveal, N11.27), status line, round ghost power button top right, "Forgot passphrase?" footer that opens the RESET row). **N11.28 (2026-08-10): the power button runs a ring first, and only the click on the finished ring quits.** Hovering the button starts it: `PillButton.set_progress()` (new in `wintheme.py`, the one native-look source, drawn by `draw_ring_progress()`) sweeps the button's own circle in the accent colour from the top clockwise over `OFF_FILL_MS` (1.1 s, `lockwindow.py`), the fill warms slightly, and **the power glyph itself turns accent only when the circle is closed**, because that is the moment the click counts. The run owns itself: moving the pointer away mid-sweep does **not** rewind it, and re-entering does not restart it. A click before the full circle does nothing at all (no quit, no message); a click on the armed button runs the unchanged exit path (`quit_app()` -> `teardown('quit')`, G35). The armed state holds `OFF_HOLD_MS` (10 s), then falls back to idle; `MouseMove` re-triggers, so a pointer still resting on the button after the timeout can arm it again without leaving first. The `icon` kind therefore has no hover colours any more (it used to flip to danger, which drowned out the ring); do not put them back. This is a safety gesture, not only decoration: the button used to quit on the very first click. While `state["quitting"]` is set the window accepts neither an unlock attempt nor the reset (never two exits side by side). The ring uses the fixed native `T.ACCENT`, because the user's accent setting lives inside the closed vault and is unreadable while locked, exactly like the big lock ring next to it. `_test_after_shown` carries `off_hover` (the trigger) next to `off`, since a synthetic click alone can no longer quit. **N11.26 (2026-08-08): the lock screen carries no text at all and must not get any back** (the two lines "NoaToDo is locked" and "Type your passphrase and press Enter." are gone; the lock says the state, the "Password" cue says the task, and Enter is the learned gesture in a password field). The `title`/`subtitle` labels still exist but are `Visible = False` unless `vault_error` is set: the N6 error screen is the **only** exception, because a damaged vault must not face a mute window, and its layout reports their height to `_ring_geometry(..., extra=...)` so the ring stays centred there too. The `Curtain` calls the same geometry without `extra`, which is exactly the normal case, so nothing jumps on handover. The password pill is smaller with the same change (320x46 instead of 380x52; **N11.27, 2026-08-10** shrank it again to **240x40** and dropped the in-pill Show/Hide entirely, see the paragraph below). The same rule holds for the WebView lock cover (`renderLock()` in `app.js`, ring only, its dead `.lock-card h2` CSS rule deleted), or a line would flash on the way over that the native window no longer has. **N11.24 (2026-08-08): there is no "Unlock" button any more and there must not be one again** (Enter in the password field is the only trigger, and `state["blocked"]` carries the rate-limit lockout that used to sit on `Button.Enabled`). The reason is structural: native controls paint their own background and know nothing about the ring's glow, so anything sitting right under the ring cuts that glow off in a hard line, which is exactly what the button did. The gap between ring and the first control below it is therefore large on purpose (`ring_gap` in `_ring_geometry`, ~half the ring width); do not put a control in there. **Unlock animation (N11.22, 2026-08-08):** a correct passphrase does not close the window silently any more, `play_unlock_anim()` runs a ~620 ms, 16 ms-tick animation on the ring (accent -> `--secure` green via `T.mix`, the lock glyph's shackle swings open around its right leg through the new `open_t` parameter of `wintheme.draw_lock_glyph`, one small size pulse, every other control hidden), then the timer sets the exit and closes the form; only the ring rect is invalidated per frame and the form is double-buffered. It holds nothing up (the vault is already open) and replaces the welcome screen on that path (N11.20). **It runs on success AND only when `resumed` is true (N11.26, 2026-08-08):** `run_lock_window(..., resumed=)` gets the flag from `api._resumed` (the same one that feeds `get_boot_state().resumed`), so a return into a locked running session opens the lock, while a **normal program start** closes the window straight away (`leave_unlocked("plain")`, so the handover curtain carries no picture either) and the app's welcome screen with the logo mark is the single greeting. Exactly one celebration per way in, never two in a row; do not let the open lock back onto the start path. Layout is recomputed on every resize and scaled by `GetDpiForWindow` (WinForms works in physical pixels under the per-monitor DPI awareness `main.py` sets; font point sizes scale by themselves). `main.py` passes `on_ready` so the lock window's taskbar entry gets the AppID + logo instead of the generic python icon. Honest limits (documented in B.3, do not "fix" them silently): Windows' own file/folder dialogs are OS-owned and stay Windows-look, the three caption buttons are Windows-drawn, the app fonts exist only as `.woff2` (GDI+ cannot load them, so native windows use the same CSS chain's last stage `system-ui` = Segoe UI Variable/Segoe UI), and native windows are always dark because the theme setting lives inside the closed vault. The `_test_after_shown` seam in `run_lock_window`/`show_message` is test-only (production never passes it).

**No plaintext reveal of a passphrase, anywhere it is entered (N11.27, 2026-08-10).** Binding, Bauplan N11.27 in the B.4 lock-screen section. Three parts: (1) the native lock window's in-pill **Show/Hide button is gone and must not come back**, and the Edge/WebView2 password eye is off on **every** password input in the frontend (`input[type="password"]:not(.pw-reveal)::-ms-reveal` plus `::-ms-clear` in `style.css`), the passphrase-change modal included. **The one exception is the first-time setup** (onboarding step 2, `#ob-pass1`/`#ob-pass2` carry the `pw-reveal` class): there is no stored secret to uncover there, and an unnoticed typo costs the whole vault because there is no recovery (N11.3). Where an **existing** passphrase is typed, it stays masked. (2) The pill is **240x40** (6:1, radius is always half the height so it reads as a pill, not a bar), and **its border sits on whole pixels**: the old 1.4px focus border on a half-pixel path made GDI+ smear the stroke across two pixel rows, which kept the straight edges crisp but left the rounded ends pale and frayed (and the field is always focused on the lock screen, so that was the permanent look). `wintheme.line_w()` rounds a stroke width to whole physical pixels (scaling with the display), `wintheme.stroke_pill()` insets the path by exactly half of it, and **both `PillInput` and `PillButton` draw through it** - it is the one source for painted pills. Do not go back to hand-rolled half-pixel offsets. Its height feeds `lockwindow._ring_geometry()` (block 382) and therefore the `.lock-card` anchor `calc(50vh - 211px)` in `style.css`: those two are a pair, change one and change the other. (3) `wintheme.PillInput` is **left-aligned** (`HorizontalAlignment.Left`), so the cue banner and the first typed character sit in the same place and nothing jumps; the old centred alignment is gone. The dead `.lock-input` CSS block was deleted with this change (the WebView cover has been ring-only since N11.18/N11.26, so those rules, including its own `::-ms-reveal` line, could never apply).

**Window handovers are seamless (N11.25, 2026-08-08).** Between two windows the screen must never go empty; the binding contract is Bauplan B.3 ("Der Fensterwechsel ist nahtlos"). The reported symptom was "the app minimizes when I press lock": it was not a minimize but a **gap with no window at all** (the WebView form is closed, `_wipe_profile_dir()` (G14) then blocks the main thread for up to ~2 s, and only afterwards does the native lock window appear; the same in reverse while WebView2 starts up). `lockwindow.Curtain` closes that gap: a maximized, app-designed form (same backdrop + grid, same DWM caption, same three caption buttons, `ShowInTaskbar=True` so the taskbar button never blinks out) that is put up **before** the old window closes and taken down only once the next one has painted. It draws **the same ring in the same place** as the lock screen, because both now take their geometry from `lockwindow._ring_geometry()` and their picture from `lockwindow._paint_ring()` (one source, no second calculation); the three modes are `"lock"` (closed lock, going into the lock window), `"unlocked"` (open lock in `--secure`, continuing the N11.22 animation on the way into the app) and `"plain"` (backdrop only, going into onboarding). Wiring: `main.py._show_curtain/_close_curtain` own the single instance, `run_webview.request_teardown` raises it when `session.next_state` is `locked`/`onboarding` (never on quit/killswitch: on exit nothing may linger), `run_lock_window` takes `on_painted` (curtain down, right after `form.Refresh()`) and `on_leaving(mode)` (curtain up, immediately before the form closes for unlock/reset), and the WebView side drops it ~250 ms after `window.events.loaded`. `create_window(background_color=wintheme.BG)` kills the white WebView2 flash under it. The curtain runs on its **own STA UI thread** with its own message loop, because the main thread is blocked during the handover and a non-pumping window would stop painting and be greyed out by Windows; it shows no user content, calls no bridge, holds no keys, does not delay any teardown step (G35), and a 15 s guard timer closes it no matter what. Do not make it interactive and do not give it content. **The switch itself is invisible too (Bauplan N11.25 point 5, added 2026-08-08 after "the switch looks ugly, it should not be visible"): the curtain closed the gap, but four seams still showed the change, and all four fixes are binding.** (a) **No Windows window animation:** every own window (WebView, lock window, curtain) gets `DWMWA_TRANSITIONS_FORCEDISABLED` via `wintheme.disable_window_transitions()`, otherwise the old window visibly shrinks away under the curtain and the new one expands back in; that flag and the dark caption are now set in `HandleCreated`, **before** the window is first shown (`style_form`), so no light Windows caption flashes. (b) **Same caption, same taskbar button:** the curtain now has `ControlBox=True` (its buttons would otherwise vanish and return on every switch) and gets `_apply_taskbar_identity` through its new `on_ready` callback (otherwise a second taskbar button with the generic python icon appears during every handover); it stays non-interactive because `FormClosing` cancels any close that is not ours and a minimize bounces straight back to maximized. (c) **Same grid, same ring position in all three views:** `wintheme.set_ui_scale()/grid_step()` scale the painted 28px grid (and the 6px title strip) by the window DPI, because native painting is in physical pixels while the WebView draws CSS pixels (unscaled, the native grid was two thirds as wide on a 150% screen and the density visibly changed mid-switch); and the WebView lock cover (`.lock-card` in `style.css`) is anchored at `max(24px, calc(50vh - 211px))`, which is exactly the y that `lockwindow._ring_geometry()` computes (block 382 = ring 184 + ring gap 96 + input 40 + 6 + caps 20 + 10 + status 26; input 40 instead of 46 since N11.27), so the ring no longer jumps ~122px when the curtain takes over. Those two numbers are a pair: change one, change the other. (d) **Soft removal over a finished picture:** `Curtain.close()` fades the form out over ~160 ms (`FADE_MS`, on the curtain's own thread, so it still returns immediately and delays nothing), the curtain reports "standing" only after a real `form.Refresh()` (not merely on `Shown`), and `main.py` sets `WEBVIEW2_ADDITIONAL_BROWSER_ARGUMENTS=--disable-features=CalculateNativeWindowOcclusion` because Chromium treats a fully covered window as invisible and stops producing frames, which left an empty surface for a moment after the curtain went away. That last one is a pure rendering switch with no security effect. **A stranded curtain must be impossible (fix 2026-08-08 after "you only see the lock and cannot enter the password"): the curtain shows exactly the ring, so if it stays up it looks like a lock screen that refuses input.** Four rules keep it from being the last window standing, all binding (Bauplan N11.25 point 4): `run_lock_window` wraps `layout()` + `form.Refresh()` so a failure there cannot skip `on_painted`; it calls `on_painted(hwnd)` a **second** time from the 320 ms timer (taking the curtain down is idempotent, `main._close_curtain` holds exactly one instance); `_call()` prints swallowed callback errors under `NOATODO_DEBUG` (silent swallowing is how a stuck curtain hides); and the curtain's caption buttons **work** (no `FormClosing` cancel), because a curtain you cannot click away locks the user out of their own vault, which beats the cosmetic cost of a click mid-handover. `Curtain.close(next_hwnd)` also hands the foreground to the next window via `SetForegroundWindow` **before** it goes, and hides itself before closing (`TopMost = False`, `Hide()`, then `Close()`): a dying, still visible, always-on-top window makes Windows redistribute activation, which need not land on the window underneath.

**Real airplane mode is DONE (N11.5, 2026-07-21).** The online/offline pill and the `G` key drive a real Windows airplane mode, not a flag. `backend/radio.py` (new) wraps the WinRT radio APIs (modular `winrt-*` packages, see Key dependencies): `set_online(false)` enumerates radios via `Radio.GetRadiosAsync()`, switches every WiFi/MobileBroadband/Bluetooth radio with `SetStateAsync` (there is no public airplane-mode flag, only per-radio switching, U14; `Other`/FM/GPS are never touched), then **reads the real state back and answers only after completion** (never fire-and-forget, U15). The contract is `{online, partial, access, refused}`: the aggregation is security-first, `online:true` as long as **any** managed radio is still on (the app never claims "dark" while something still transmits); `partial:true` when not every target radio obeyed, and `refused` names the first disobedient radio so the pill tooltip can say e.g. "Bluetooth could not be turned off" (there is **no toast**, N11.16, the honest pill is the message). Denied `RequestAccessAsync` (or missing packages) degrades visibly: tooltip "no radio access", **no** radio touched, real state left standing, `access:'denied'`/`'unavailable'` (never a false offline claim, B.10, this is a privacy/convenience tool, not a security bolt). **At most one radio op at a time** (a non-blocking lock in the controller; the frontend also guards with a `netBusy` waiting state on the pill). **External changes are mirrored event-driven** (`add_state_changed` per radio -> `Api._on_radio_external` -> `window.noa.onNetChange`); the 10 s `get_wifi_signal` poll doubles as the rare counter-check fallback (it returns the reconciled `online`, and only runs while online + window visible + unlocked). **While a panic full screen runs** (wipe progress, end screen, killswitch progress) both mirrors update `state.online` but deliberately skip `render()` (fixed 2026-07-25): the panic confirm switches the radios off for real, so its own radio event landed inside the running progress bar and rebuilt the DOM, freezing the bar and resetting the percentage to 0. `runPanicProgress` additionally keeps its position in `state.panic.progress`/`stepText` and re-queries its DOM nodes every frame, so no re-render from any source can tear the run apart again. **N11.10 still holds: locking never touches the radios**; radios are switched only by the explicit user toggle and by the **panic** flow (which now switches off for real). The pre-app radio state is remembered in `config.json.radio_baseline` the first time the app goes offline (so a crash cannot leave the machine dark forever); `teardown` step 10 (`main.py:_restore_radio_if_needed`, an **exit-only** step) restores it and clears the marker, and a leftover marker is also restored once at the next startup (crash recovery). Verified on real hardware (WiFi + Bluetooth actually toggled off and back on, baseline persisted, not overwritten on re-toggle, restore + clear).

Locally-usable milestone reached (Phases 1 to 7 of the Bauplan are done; Phase 7 finished 2026-07-17, see the "Phase 7 is DONE" paragraph below. Phase 6.5 UX/security follow-ups: inline task edit via double-click, task delete via the rail trash button (acts on the selected task; **there is no per-task hover trash on the card** and none will be added, decided 2026-07-17 with Phase 7; the unused `.t-del`/`del-task`/`.t-grip`/`.title-row`/`.airplane-pill` leftovers were deleted, Plananalyse S7/Audit 1.6), click-to-select tasks, hardened single-task copy via `copy_task` (gate G23: backend-side Win32 clipboard, excluded from Win+V history and cloud clipboard, auto-clear after 60 s), Ctrl+C app shortcut removed entirely, contextual rail pencil (selected task: inline edit; otherwise: rename list), mini mode always-on-top). Implemented: `db.py`, `api.py`, `main.py`, `frontend/index.html`, `frontend/style.css`, `frontend/app.js`.

Screenshot protection (gate G26, `SetWindowDisplayAffinity` / `WDA_EXCLUDEFROMCAPTURE`) is **removed from the code and should stay removed** (there is no screenshot code anywhere in `main.py` or `backend/`; commit 45820c2 took out the last attempt). It caused recurring problems: on some GPU/driver combos the affinity flag blocks WebView2 from rendering at all (window stays white / "not responding"), and its startup wiring previously deadlocked the message loop (a blocking `_get_hwnd(window, wait=True)` plus a cross-thread native call on the on_start worker thread). It also blacks out the window in legitimate screen sharing/recording and does nothing against a phone camera. Do not reintroduce it.

**WebView2 profile + single instance (gates G14 partial, G19 done, pulled forward 2026-06-20):** `main.py` no longer runs WebView2 in private mode. It starts PyWebView with `private_mode=False, storage_path=PROFILE_DIR` where `PROFILE_DIR = %LOCALAPPDATA%\NoaToDo\webview`, a single fixed, user-private profile folder. This replaced the old per-start temp profile under `%TEMP%\tmp...\EBWebView` that piled up on hard exit (real: dozens of leftovers, start hangs over a minute). `_cleanup_stale_webview_profiles()` removes those old temp profiles once at startup (only `tmp*` dirs carrying an `EBWebView` signature; locked ones are skipped). A named single-instance mutex (`_acquire_single_instance`, `Local\NoaToDoSingleton`, gate G19) makes a second instance show a message box and exit; the fixed folder is only safe together with this mutex (two instances would lock/corrupt the shared profile). **Open rest of G19 (V3, 2026-07-15):** the mutex name must move to `Global\NoaToDo-<User-SID>`; a `Local\` mutex is unique per logon session only, so the same user via RDP or fast user switching could start a second instance on the same DB (exactly the corruption G19 exists to prevent). The code still uses `Local\` today; rename at the latest in Phase 8. The profile holds only non-sensitive UI cache (own HTML/CSS/JS/fonts, GPU state), never task content. **Done in Phase 8 (G14):** `main.py`'s boot loop wipes `PROFILE_DIR` (`_wipe_profile_dir`) after every window teardown (lock/autolock/panic/quit incl. the window X, which now runs the same `teardown("quit")` path via a `FormClosing` handler); orphaned `msedgewebview2.exe` whose command line points at `PROFILE_DIR` are killed first (`_kill_orphaned_webview2`) so the wipe doesn't hit `0x800700AA` ERROR_BUSY; verified real (after a lock, `PROFILE_DIR` no longer exists). The mutex namespace moved to `Global\NoaToDo-<User-SID>` (G19/V3). The one remaining G14 piece is Phase 9's first-start cleanup of the old redirected paths in the `.exe`. Do NOT reintroduce `private_mode=True`. **N11.18 (spike U3):** the lock screen is a native WinForms window (`lockwindow.py`), so there is no `LOCK_PROFILE_DIR`; the G14 wipe target is met because the native lock window holds no WebView open on `PROFILE_DIR`.

**Stale frontend cache (fixed 2026-06-23):** the fixed profile introduced a regression the old per-start temp profile masked: WebView2 caches the `file://` frontend (`index.html`/`app.js`/`style.css`) in `PROFILE_DIR\EBWebView\Default\Cache`, and that cache now survives restarts, so an old frontend is served for hours up to ~a day (Chromium heuristic freshness on files without cache headers). Symptom: "the old version still runs despite closing and restarting". `_purge_webview_cache()` in `main.py` fixes it by deleting only the `Cache` and `Code Cache` dirs under `PROFILE_DIR` on every startup (GPU/shader state is kept); it runs right after `_cleanup_stale_webview_profiles()`. Do not "optimize" this away. CSP `script-src 'self'` rules out an inline cache-busting bootstrap, so the disk purge is the clean fix. Note: the app is typically run via a venv on Microsoft-Store Python, whose `%LOCALAPPDATA%` writes are redirected, so the real folder lives under `...\Packages\PythonSoftwareFoundation.Python.3.11_*\LocalCache\Local\NoaToDo\webview`; in-process `os.walk(PROFILE_DIR)` sees the redirect automatically, external tooling looking at the literal path does not. **V8 (2026-07-15, Bauplan G14 + N11.15.5):** any wiping therefore always operates in-process on the effective path, and the Phase 9 `.exe` (which runs without the redirect) gets a one-time first-start step that removes the known old redirected leftovers (the redirected `NoaToDo\webview` folder and its `config.json`, never a `tasks.db.enc`).

**Microsoft To Do sync removed (2026-07-09):** the app is purely local with no cloud/sync of any kind. The former `backend/auth.py` (MSAL login) and `backend/graph_sync.py` (Graph sync) files were deleted, and the `sign_in`/`sign_out`/`sync_now` bridge methods, the `synced`/`source`/`graph_etag` columns, the `sync_state` table, and `due_at` were all removed. The online/offline (airplane) toggle stays; **as of 2026-07-21 (N11.5) it is a REAL Windows airplane-mode control** (`backend/radio.py`): `set_online(false)` switches off the actual radios (WiFi/Bluetooth/mobile) via the WinRT radio APIs, mirrors external changes back into the UI (event-driven), and restores the pre-app radio state as the **last** shutdown step (after the room is cleared). See the dedicated airplane-mode paragraph below.

**Phase 7 is DONE (2026-07-17):** the export is two-step (scope: current list via `export_list` or all lists via `export_all`, then format `md`/`txt`; JSON dropped, N11.1.5) behind a pill next to the rail, the backend shows a real save dialog and writes the file (gate G21: sanitized filename suggestions, one-line tasks, UTF-8 no BOM + CRLF, cancel -> silent `canceled`, one native dialog at a time via `_native_dialog()` -> `busy`; the `exportDone` setting, Default on, decides whether completed tasks are written, N11.2.4/U10 Punkt 6); `move_task(id, target_list_id)` and `reorder_lists(ordered_ids)` exist with the N11.2.2 edge cases (sidebar drag and drop, task drag onto a sidebar entry, right-click "Move to…" menu); list deletion has the Undo toast (N11.2.1, RAM buffer); gates G20, G21, G22, G29, G12 and G23 are all done. **Phase 8 is now built too** (see the "Phase 8 is DONE" paragraph at the top of Project status): `backend/security.py` is the real security core, no longer a stub.

**Notifications removed (2026-07-09):** the app has no notifications of any kind (neither in-app alerts nor Windows toasts). The former `backend/notify.py` (winotify) file was deleted, the `winotify` dependency dropped, the `notify`/`notifyInApp`/`notifyWindows` settings and their Settings-modal section removed, and the `onNotification` backend -> frontend event removed. **As of 2026-07-17 (decision N11.16, user wants no notifications) the app has exactly ONE toast left: the list-delete Undo toast** (`pushUndoToast`, N11.2.1, bottom-left next to the sidebar, ~6 s, with a countdown bar). Everything else that used to pop up was removed in two steps: first all **success confirmations** ("List created", "Task updated", "Task deleted", "List renamed", "Task moved", "Back online"/"Offline mode", "Exported", "Task copied", "List restored", "Errors copied"), then all **error and validation toasts** (the whole B.2/G29 per-code toast policy, plus the "Task text cannot be empty" / "Select a task first" guards). The generic `pushToast` helper and its `.toast`/`.toast-wrap` CSS were deleted. A successful action is no longer acknowledged; errors run silently or stay in the status modal's "Recent errors" (G29). **Do not re-add any toast** except the Undo one. The former "Phase 8 (notifications)" no longer exists; lock/panic/encryption is now Phase 8.

**Planned per Bauplan N11 (2026-07-09), decided but NOT yet in code (do not assume these are implemented; implement them per the Bauplan, do not re-add what N11 drops):**
- **Task `meta` field removed entirely (N11.1.3, DONE 2026-07-17):** a task is only `text` + `done`. The DB column, the `add_task`/`edit_task` meta arg, the render, the inline-edit meta input, and the export meta are all gone. `db._drop_legacy_columns()` drops `meta` (and the orphaned sync-era columns `synced`/`source`/`graph_etag`/`due_at`) from existing dev DBs once at connect. Do not re-add any of them.
- **Export is `md`/`txt` only (N11.1.5), two-step (scope then format), plus `export_all` (N11.2).** JSON export is dropped.
- **`set_online` is a real Windows airplane-mode control (N11.5, DONE 2026-07-21),** see the airplane-mode paragraph below.
- **Locking no longer switches offline (N11.10, DONE 2026-07-21):** every lock (lock button, `Ctrl+L`, auto-lock) leaves the online/radio state exactly as it is (no airplane mode on, no restore). `clearWorkspace()` no longer touches `state.online`; only the panic-confirm path sets offline. Radios are switched only by the explicit user toggle (pill/`G`) and by the panic flow; the pre-app radio state is restored only on quit (`teardown` step 10, a no-op until N11.5 builds the real airplane toggle).
- **Theme auto-follows Windows (N11.6, DONE 2026-08-08):** the `dark` setting key is replaced by `theme` = `auto|light|dark` (default `auto`); auto mirrors the OS light/dark state live (event-driven, plus a 60 s fallback re-check). `Ctrl+J` sets a manual override: from `auto` it switches to the opposite of the currently shown (effective) theme, from a fixed theme to the other fixed theme; it never returns to `auto` on its own (only the Appearance settings segment does). The interval and the two `Ctrl+J` rules resolve Plananalyse U16 (2026-07-15). See the theme paragraph in Project status for the built shape.
- **New settings keys (DONE):** `sound` (bool, completion sound on/off, default on, UI built 2026-08-08) and `autoLock` (minutes to auto-lock, `0` = never, default `15`). The `toolbar` key is retired (its settings row is deleted by `db._migrate_settings()`).
- **Vault file location is chosen by the user on first run (N11.3);** its path lives in a small unencrypted config (e.g. `%LOCALAPPDATA%\NoaToDo\config.json`), never inside the vault.
- **Passphrase policy (N11.3/N11.4):** minimum length 12 (no other rules) with a prominent "no recovery" warning; **no pepper recovery export** (the vault is bound to this Windows account, forgetting the passphrase means data loss); a lock-screen **Reset** path (killswitch-style confirm, then type `RESET`) wipes the vault and restarts full onboarding; passphrase changeable in Settings; unlock rate-limit is 2 s after each wrong try, 3 free tries, then an escalating lockout ladder 10 s / 30 s / 1 min / 5 min / 15 min / 30 min / 1 h / 5 h / 10 h (each stage allows 2 tries before advancing).
- **`move_task(id, target_list_id)` and `reorder_lists(ordered_ids)`** are added in Phase 7 (N7). No `clear_completed`, no full-text search (both dropped).
- **The window starts maximized (N11.6).**

**As of Phase 8 (2026-07-21) the encryption is real and both layers are always present.** `DEV_AES_KEY` and the `db.connect()` default are gone (G9); `db.Database.__init__` requires 32 raw key bytes and sets them as a raw hex key (`PRAGMA key = "x'...'"`, G7). There is no working `data/tasks.db` at rest anymore; the only rest artifact is `tasks.db.enc` at the user-chosen vault path (ChaCha20-Poly1305 over a SQLCipher-AES-256 image, G16). The runtime working copy is a SQLCipher-**encrypted** file under `%LOCALAPPDATA%\NoaToDo\work` (N11.9 fallback, never plaintext), snapshotted via `VACUUM INTO`; it is securely deleted on lock/quit. `api.lock/unlock/panic/killswitch/quit_app` are real and run through the one `teardown(reason)` sequence; the lock is enforced backend-side as a G13 allowlist in the `@bridge` decorator. **Honest status (G22):** encryption being real, `get_status()` reports the real active state (`encryption.active: true`, both layers, Argon2 params, `pepper` present, `last_wrap`, real BitLocker status or honest "unknown"); the `dev_key` flag is gone. **The status modal was wired to those values on 2026-07-25** (it had kept the hardcoded Phase-7 rows "tasks.db" / "AES-256 · dev key, layer 2 pending" while the backend already answered honestly, so the UI understated the real state): `renderModal()`'s `status` case now builds **every** row from the freshly fetched `get_status()` (Vault file + size, layer 1, layer 2, key derivation, pepper, last wrap, disk encryption/BitLocker, network, WebView2 version) plus the G29 "Recent errors" section. There is no fixed status text in the frontend any more: a pending answer renders "checking...", a failed call "unavailable" (`statusFailed`), and a value the backend cannot read renders "unknown" (BitLocker without WMI access, missing WebView2 registry entry), never a claim. `encryption.last_wrap` is the mtime of `tasks.db.enc`, which is exactly the last wrap because only `wrap_to_file()` writes that file. `.modal-status` gives the now taller modal the same scrolling body as `.modal-settings`. **Still open (not built, needs a decision):** the N11.11.5 bullet "the parked-cleanup state must be visible in the status modal" cannot be satisfied as written, because `get_status` is not in `ALLOWED_WHEN_LOCKED` and the parked state only exists while locked; a row for it would always read "clean" and could never show the other value. The panic wipe/end screens keep the honest texts ("Clearing workspace" / "Workspace cleared", N11.17, permanent); the deliberate false "All data securely wiped" outward claim will **not** be built, so G22 applies with no exception (B.10.5). Killswitch/Reset are now real file operations (delete `tasks.db.enc` + `.bak` + config + pepper) via the teardown sequence.

**Strengthened lock, lock-screen off button, panic end screen + killswitch (Bauplan Nachtrag N10, UI implemented 2026-07-08):** Locking (button or `Ctrl+L`) now first "clears the room" like panic (`clearWorkspace()` in `app.js`: lists/selection/menus/modals discarded in memory, sidebar closed, offline), then shows the lock screen; nothing is deleted, unlocking re-fetches everything via `get_state()`. (The "goes offline / stays offline until re-enabled" part is superseded by N11.10, 2026-07-13: locking must no longer touch the online/radio state at all; that is today's code behavior still to be changed, see the N11 list above.) The lock screen has a power button top right (`quit_app()`: closes the WinForms form via `BeginInvoke` on the UI thread; quits without a passphrase, never deletes data). The panic flow no longer returns to the app: confirm -> real cleanup + wipe progress screen -> end screen with two exits (end-screen text since 2026-07-17: honest "Workspace cleared" per G22; permanent per decision N11.17, the deliberate false claim "All data securely wiped" that N10.3 had planned for Phase 8 will not be built, B.10.5): **Finish** (accent, just quits) and **Killswitch** (gray, two-stage in-button: first click slides the label right and an "OK" slides in, handled in-place without re-render so the CSS transition survives; "OK" runs `api.killswitch()` -> `db.killswitch()`, which really and irreversibly deletes all lists/tasks/settings, rewrites default settings, sets the `seeded` marker, runs `PRAGMA secure_delete` + `VACUUM`, then the app quits itself). Next start behaves like a first run without demo data: `seed_if_empty()` skips seeding when `settings.seeded == "true"` (the marker is also written on a normal first seed). The Phase 8 duties remain: secure `PROFILE_DIR` wipe on the `quit_app()` path (G14), key zeroing (G25), and G13 must enforce the lock as an explicit allowlist, `ALLOWED_WHEN_LOCKED = {"unlock", "quit_app", "killswitch", "get_state", "get_boot_state", "choose_vault_dir", "create_vault", "reset_vault"}` (the last four added by the U1 decision, N11.13: onboarding and reset run precisely **without** keys and would otherwise be blocked; `change_passphrase` is deliberately NOT in the list, it needs the unlocked state). Everything else, including `lock`/`panic`, returns `{"error": "locked"}` while locked; `get_state()` returns only `{"locked": true}`. Allowlist, not "every method except X": any newly added bridge method is denied by default and has to be allowed deliberately.

**Gate table bookkeeping (fixed 2026-08-10):** the normative B.9 gate table still said "offen" for the Phase-8 gates (G6, G7, G8, G9, G13, G14, G15, G16, G17, G18, G19, G25, G28, G32, G35) although the non-normative quick overview and this file had recorded them as done on 2026-07-21. Phase 8 had updated the summary but not the normative source, which is exactly what B.9's Redaktionsregel forbids. The rows now carry the Phase-8 status plus, where it exists, the Phase-9 test evidence. G27 and G34 are `teils erledigt` on purpose: signing is missing (no certificate, N11.29 b) and the G34 key checks at the running window are still a manual step.

**Security gates:** the Bauplan defines mandatory security gates in section B.9. Since the Struktur-Umbau (Etappe 2, 2026-07-16) they live in **one** merged table sorted by gate number; the former split into an original set plus the audit addendum "NACHTRAG: Gates G13 bis G35" is history, recorded in the Entscheidungsregister (Anhang 1, "Herkunft der Sicherheits-Gates"). Some gate rows carry only a short title plus a full-text anchor into their contract (list in the B.9 head: G13/G20/G29 in B.2, G14/G35 in B.8.5, G16/G28 in B.7, G21 in Phase 7, G27/G34 in Phase 9, G30 in B.10); the other rows still hold their full text inline. G28, the Phase 8 encryption proof, comes from N11.9. The sync/login-only gates (G1-G5, G10, G24) were removed together with the Microsoft integration. None of the remaining gates are optional. When implementing any phase, read its gate list in the Bauplan first; the quick overview table is at the end of the Bauplan.

**Gates G31 to G34 + G27 extension + window-title rule (adopted 2026-07-15 from Plananalyse findings A1 to A7; normative full text is each gate's row in the B.9 table, except G34: since Umbau-Etappe 6 its full text lives in the Phase 9 gate block, the B.9 row keeps status/date/check plus the full-text anchor):**
- **G31 (Phase 8, RAM-to-disk leaks, A1; DONE, setup-UI part 2026-08-08):** BitLocker recommendation in the setup UI plus the real BitLocker status in the status modal (an unreadable WMI query shows "unknown", never a false "protected"); all key `bytearray`s (aes_key, chacha_key, master secret, pepper) locked with `VirtualLock` after derivation and `VirtualUnlock`ed before the G25 zeroing (best effort; documented limit: does NOT keep keys out of `hiberfil.sys` or crash dumps, only BitLocker covers those); no `faulthandler` file target, no traceback/dump files (matches G29).
- **G32 (Phase 8/onboarding, vault location, A2):** onboarding defaults to `%LOCALAPPDATA%\NoaToDo`; a chosen path under a detected sync root (OneDrive env vars, Dropbox `info.json`, path-component heuristic, best effort) shows a prominent warning naming both facts: the provider keeps version history, and **killswitch/reset do not delete cloud versions**. A warning, never a block.
- **G33 (Phase 8, dev legacy data, A3; DONE, notice 2026-08-08):** the first `create_vault()` secure-deletes the old `data/tasks.db` including `-journal`/`-wal`/`-shm` (overwrite then unlink, same path as the `.bak` cleanup in N11.3 (c), never a bare `os.remove`), plus a one-time notice about the honest SSD forensic limit (wear leveling), shown as the must-acknowledge onboarding step 4 and only when something was really removed.
- **G34 (Phase 9, release hardening, A4/A6):** the frozen release build hard-ignores `NOATODO_DEBUG` (build constant) and sets `AreDevToolsEnabled=false`, `AreBrowserAcceleratorKeysEnabled=false` (kills the Ctrl+P plaintext-PDF export past G21) and `AreDefaultContextMenusEnabled=false`. **`text_select=False` is now set explicitly in `create_window` (done 2026-07-16, `main.py`);** the former unintended PyWebView default is a deliberate, commented decision now. The regression test comes with the Phase 9 test list (no test setup yet). The rest of G34 (a)/(c) stays Phase 9. Input-field copy stays an open, honestly documented channel (B.10.3 item 8).
- **G27 extension (Phase 9, frontend integrity, A5):** the exe signature does not cover `index.html`/`app.js`/`style.css`; embed the frontend assets in the signed binary or verify each asset hash against an embedded manifest at startup, and refuse to start with a clear message on any mismatch (hardens against silent K4 persistence, never sold as full K4 protection).
- **Window-title rule (B.4, A7, no gate):** the native window title is constantly "NoaToDo" and never contains user content (no list names, task text, or counters, in no mode incl. mini mode); same for taskbar tooltip and jumplists.

## Building the release (`NoaToDo.exe`)

```powershell
# From Code/ (venv python), one-file release build:
.\venv\Scripts\python.exe tools\build_exe.py
# Debug variants (never shipped): folder build / with console
.\venv\Scripts\python.exe tools\build_exe.py --onedir --console
```

`tools/build_exe.py` is the **one** entry point (Bauplan N11.29 a2): it refuses to run on anything but CPython 3.11.x (G11/U25), writes the build stamp `Code/_buildstamp.py` (build date, commit with a `+dirty` mark, `SIGNED = False`, the G27 frontend SHA-256 manifest), writes the version resource `Code/build/version_info.txt`, runs PyInstaller with `Code/NoaToDo.spec`, and **deletes the stamp again** in a `finally` (a leftover stamp would make the next dev start run the integrity check and cry wolf on every frontend edit). The spec builds one-file with `optimize=2` (no docstrings, no asserts, G27), embedded icon, `console=False`, **no UPX** (packed binaries are an antivirus red flag and buy no security), and excludes `pytest`/`PyInstaller`/`tkinter` from the bundle. `--onedir` is the only way to run the G27 tamper proof by hand (only there does an `app.js` lie next to the exe). Dev tooling is pinned in `Code/requirements-dev.txt`. **G11 is complete since 2026-08-10:** `Code/requirements.lock.hashes.txt` carries a `--hash=sha256:` for every pinned package (generated by `tools/lock_hashes.py`, which downloads each artifact once and hashes it; it is deliberately *not* part of the build, since hashes only change when versions change), and the release install is `pip install --require-hashes -r requirements.lock.hashes.txt` (verified as a dry run over all 27 packages). Build output (`Code/build/`, `Code/dist/`, `_buildstamp.py`) is git-ignored. **Verified 2026-08-10:** one-file exe 19.5 MB, starts on this machine, version + icon in the Windows properties; the tampered one-folder build stops at the integrity message and opens **no** WebView (zero `msedgewebview2` processes on the NoaToDo profile).

## Running the app

```powershell
# From Code/ directory (venv must be active or use the venv python directly):
.\venv\Scripts\python.exe main.py

# Enable WebView2 DevTools (main.py reads this env var):
$env:NOATODO_DEBUG = "1"; .\venv\Scripts\python.exe main.py
```

`Code/run.ps1` is a convenience launcher: it always starts `main.py` with the project's own `venv` python regardless of the current directory, so you can double-click or call it from anywhere.

No build step, the frontend is vanilla HTML/CSS/JS loaded directly by PyWebView. There is no hot-reload: frontend edits need a full app restart (close the window AND make sure the python process is gone, a still-running instance also blocks relaunch via the single-instance mutex). The WebView2 cache is purged on each startup (see "Stale frontend cache" above), so a clean restart always shows the latest code. `main.py` prints `[NoaToDo] Start. Frontend: index.html HH:MM:SS, app.js ..., style.css ...` at launch; if those mtimes do not match your last edit, an old window is still running.

**Tests (Phase 9, since 2026-08-10):** `Code/tests/` with pytest, run from `Code/` via `.\venv\Scripts\python.exe -m pytest` (63 tests, ~4 s; `pytest.ini` sets `testpaths`). **Binding rule of that test base:** `tests/conftest.py` redirects everything that would be real user data *before every test* (`%LOCALAPPDATA%` to a temp dir, so `config.json` and the work dir follow; `config_path()` explicitly; the DPAPI pepper to a fixed test value) and makes `keyring.get/set/delete_password` raise `AssertionError` — a test that touches the real credential store could delete the user's pepper and make their vault unreadable forever. Argon2id runs at 64 MiB / t=1, the low end of the *allowed* range (N11.4.3), never outside it. Files: `test_db.py`, `test_api_validation.py` (G20/G21), `test_lock_allowlist.py` (G13), `test_crypto.py` (G15/G16/G18/G28/U8c), `test_ratelimit.py` (N11.4.1), `test_teardown_killswitch.py` (G35/G25/B.8.7), `test_release.py` (G34/G27/G29/CSP/`esc()` plus four regressions that must never come back: `DEV_AES_KEY`, `SetWindowDisplayAffinity`, `private_mode=True`, the Win+L hook; that check tokenizes and ignores comments and strings, so it does not punish the documentation that keeps them out). Honest gaps, listed in Bauplan N11.30: the XSS test is the static one (no interpolation of foreign values without `esc()`), the running-DOM variant still needs the CDP path; and everything that needs a real window (G6 file monitor, G17 hard kill, G19 second instance, native dialogs, the G34 key checks) is covered by the manual checklist in Bauplan N11.31, not automated. There is still no lint/typecheck config. Verify UI changes by running the app.

**First run:** if `data/tasks.db` does not exist, `db.seed_if_empty()` writes only the default settings plus the `seeded` marker; **no demo lists or tasks are created**, the app starts empty. Delete `data/tasks.db` to reset to that empty first-run state.

## Architecture

NoaToDo is a **local-first Windows desktop app**: a Python backend and a vanilla JS frontend run together in a single native window via **PyWebView** (uses Windows WebView2, no bundled Chromium).

All application code lives under the `Code/` subdirectory (the repo root holds only `CLAUDE.md`, `Code/`, and `Planung/`). Paths below are relative to `Code/`; this is also why every command runs from `Code/`.

```
Code/                      # run everything from here
├── main.py                # entry point
├── buildinfo.py           # version, release switch, bundle paths (Phase 9, N11.29)
├── lockwindow.py          # native lock screen (no WebView, N11.18) + the handover Curtain (N11.25)
├── wintheme.py            # the ONE native look: tokens, DWM titlebar, pill controls (N11.21)
├── requirements.txt       # loose deps; requirements.lock.txt is the pinned set
├── frontend/              # HTML + CSS + JS, rendering only, holds no truth
│   ├── index.html
│   ├── style.css          # CSS extracted 1:1 from Planung/weiteres/NoaToDo UI Konzept.html
│   ├── app.js             # all UI logic, calls pywebview.api.*
│   ├── icon.ico           # window icon (generated by tools/make_icon.py)
│   └── fonts/             # JetBrains Mono + Space Grotesk as local .woff2
├── backend/
│   ├── api.py             # js_api class, the bridge (see Bridge API below)
│   ├── config.py          # unencrypted config.json (B.11): vault path, rate limit
│   ├── db.py              # SQLCipher CRUD
│   ├── ostheme.py         # Windows light/dark state + watcher (N11.6)
│   ├── radio.py           # real Windows airplane mode via WinRT (N11.5)
│   └── security.py        # lock/unlock/panic, key derivation, teardown (real)
├── data/
│   ├── tasks.db           # current working DB (SQLCipher-AES-256, no outer wrapper yet)
│   └── tasks.db.enc       # Phase 8 target: ChaCha20-Poly1305(SQLCipher blob)
└── tools/
    └── make_icon.py       # one-off build tool: generates frontend/icon.ico from logo
```

## Implementation details

**`@bridge` decorator (`api.py`):** wraps every public `Api` method. Catches `KeyError` as `"not_found"` and any other exception as `"internal"`. All bridge methods return a JSON-serializable dict or `{"error": code, "message": ...}` on failure.

**Error hygiene (gate G29, implemented 2026-07-17):** the frontend only ever sees a **code plus a static English text** from the canonical error-code table in Bauplan B.2 (`not_found`, `invalid`, `locked`, `passphrase`, `rate_limited`, `vault`, `canceled`, `busy`, `memory`, `internal`), never `str(exc)`, never paths, tracebacks, SQL fragments, task text, passphrase or keys. `ERROR_MESSAGES`/`_err()` in `api.py` hold the catalog; the `@bridge` decorator maps `KeyError` -> `not_found`, `MemoryError` -> `memory`, everything else -> `internal` plus a 4-hex `ref`. Details go into the **redacted in-memory ring buffer** `Api._errors` (deque, last 50 entries, `_redact()` replaces paths by `<path>` and caps at 200 chars, never bridge arguments), viewable in the status modal ("Recent errors", collapsed, copy button via the hardened G23 clipboard path, bridge method `copy_errors()`) and cleared on `lock()`/`panic()`/`killswitch()`/`quit_app()` (Phase 8 moves that into `teardown()`). Frontend: `handleError(res)` in `app.js` is the single error sink. **As of 2026-07-17 (decision N11.16, user wants no notifications) no error code produces a toast at all:** `not_found` silently `refreshState()`s (the view was stale), `locked` shows the lock screen, and `invalid`/`busy`/`internal`/`canceled` are silent (an `internal` error, with its `ref`, is still recorded in the ring buffer and visible in the status modal's "Recent errors"). The former per-code toasts (`not_found`/`invalid`/`busy`/`internal`) are gone; the generic `pushToast` helper was deleted. Do not re-add error toasts. Logging policy: **no persistent logfile in the release** (no `FileHandler`, no `basicConfig(filename=...)`, no traceback file); verbose diagnostics only behind `NOATODO_DEBUG`, and even then never passphrase/keys/task text; the release-build check is Phase 9.

**PyWebView introspection rule:** PyWebView recursively scans all public attributes of the `js_api` object to build the JS bridge. Any attribute that is not a plain method (e.g. the `Window` object stored in `api._window`) must have a `_` prefix; otherwise PyWebView descends into it and may call `evaluate_js` before the window is ready, causing "Main window failed to start". All private state in `Api` uses `_` prefix for this reason.

**Navigation lockdown (gate G12, implemented 2026-07-17):** `_wire_navigation_guard()` in `main.py` attaches a `NavigationStarting` handler to the WebView2 control (`window.native.browser.webview`) that cancels every navigation except `about:` and `file:` URIs inside the app's own `frontend/` dir; `webview.settings["OPEN_EXTERNAL_LINKS_IN_BROWSER"] = False` makes `window.open` go through `load_url` (and thus the same guard) instead of the system browser. The app is purely local and never navigates elsewhere; do not loosen this. One deliberate exception (loopback, corrected 2026-07-17): PyWebView 5.x does **not** serve the frontend via `file://` at all, it serves it over its own local HTTP server (`http://127.0.0.1:<port>/`) in **every** mode, not only under `NOATODO_DEBUG`. The guard therefore allows `http://` on loopback (127.0.0.1/localhost/::1) **unconditionally**, never gated on debug: the earlier debug-only gating (added during the Phase 7 acceptance, based on the wrong assumption that only debug uses the HTTP server) left the normal window black (the app cancelled its own startup navigation) and would have blacked out the release build too (G34 makes the release hard-ignore `NOATODO_DEBUG`, so `_debug_enabled()` is `False` there, so a debug-gated exception vanishes). Only loopback is allowed; every **remote** `http`/`https` target (the real exfiltration vector G12 guards against) is still refused, so the security intent is unchanged.

**WinForms thread safety:** `set_mini` runs in PyWebView's API worker thread. All native window mutations (size, position, `TopMost`, `FormBorderStyle`) must be dispatched to the WinForms UI thread via `form.Invoke(Action(work))`. Direct calls from the worker thread cause the message loop to deadlock.

The same applies to `on_start` and the `_on_setting_change` / `_on_frame_changed` callbacks in `main.py`: they also run on a worker thread. Setting `window.native.Icon` (in `_apply_window_icon`) directly from there deadlocked against the UI thread while it was still initializing the WebView2 control (`edgechromium.py:__init__`), so the window never appeared (intermittently white, "not responding", or nothing, depending on timing; root cause found 2026-06-13 via thread stack dump). Fix: all startup window operations (icon, DWM titlebar theme, `SetWindowPos`) are dispatched through `_run_on_ui_thread(window, work)`, which uses `window.native.BeginInvoke(Action(work))` (async, non-blocking) after the window handle exists. Never call WinForms members on `window.native` directly from a worker thread; route them through `_run_on_ui_thread`.

**`db.edit_task` SQL:** builds a dynamic SET clause from a whitelisted `allowed` set (`{"text", "done"}` since the N11.1.3 meta removal). The f-string in the query is safe because only those whitelisted column names can appear. This is an intentional tradeoff and not a SQL injection risk.

## Critical constraints

**Dual-layer encryption (mandatory, both layers always present in Phase 8+):**
- Layer 1, SQLCipher (AES-256): the package is **`sqlcipher3-wheels`** (imported as `import sqlcipher3`), not `sqlite3`, `sqlcipher3-binary` has no Windows wheels, `sqlcipher3-wheels` provides them with an identical API. Immediately after `connect()`, set `PRAGMA key = ?` with the derived `aes_key`, then `PRAGMA foreign_keys = ON`.
- Layer 2, ChaCha20-Poly1305 (outer wrapper, `cryptography` package): the permanent file on disk is `data/tasks.db.enc` = ChaCha20-Poly1305(SQLCipher blob). On unlock: unwrap -> write SQLCipher working copy to `%TEMP%` (restricted permissions) -> open with `aes_key`. On lock/quit/panic: re-wrap to `tasks.db.enc` -> securely delete the working copy -> discard keys from RAM. Wrap hardening per G16/V1 (2026-07-15): the full `.enc` header goes into the AEAD as `associated_data`, the fresh `.tmp` is test-decrypted **before** the `.bak` rotation (so two bad write cycles cannot destroy both generations), and free disk space is checked before each wrap (on shortage the old state stays untouched, error code `vault`).
- Both keys (`aes_key`, `chacha_key`) live only in RAM while unlocked; never written to disk.

**Passphrase / key derivation (gate G15, N11.3):** User passphrase bound to the DPAPI pepper first, then Argon2id -> one 32-byte master secret -> HKDF-SHA256 with separate `info` labels -> `aes_key` and `chacha_key` (domain separation, not raw slices). **Pepper binding (V2a, decided 2026-07-15):** `argon2-cffi` does not expose Argon2's keyed `secret` parameter, so the pepper cannot go in that way; instead `ikm = HKDF-Extract(salt=pepper, ikm=passphrase_utf8)` (by definition `HMAC-SHA256(key=pepper, msg=passphrase_utf8)`, 32 bytes) runs before Argon2id, and Argon2id derives the master secret from `ikm` + the header salt. The construction is versioned via the G16 header format version. **Fixed Argon2id parameters (U17/N11.4.3, decided 2026-07-15, pro security): type Argon2id, version 0x13, `memory_cost=262144` KiB (256 MiB), `time_cost=3`, `parallelism=4`, `hash_len=32`, 16-byte per-vault salt.** 256 MiB, not 512, on purpose: availability counts as a security goal (a pepper-bound vault must not lock itself out on a RAM-tight machine), and the offline attacker is stopped primarily by the DPAPI pepper, not the memory cost. These params live in the G16 `.enc` header (authenticated as AEAD `associated_data`, V1) and are validated against an accepted range (64 to 512 MiB, else the header counts as unreadable -> `vault`, no Argon2 run) before allocation, which blocks a header-inflation DoS. They are raised to the current target only on passphrase change (N11.3 (d)). A `MemoryError` during derivation (in `unlock`/`create_vault`/`change_passphrase`) is caught before the `@bridge` `internal` catch-all and returned as the dedicated error code **`memory`** ("Not enough memory. Close other apps and try again."): never surfaced as wrong passphrase, never crashes, and it does NOT advance the rate-limit ladder (a memory shortage is not a guess). Concrete derivation (U18, so two implementations agree bit-for-bit): `HKDF-SHA256(ikm=master_secret, salt=None, info=<label>, length=32)` called twice with the same master secret; the two fixed, versioned labels are `b"noatodo/aes-key/v1"` (aes_key) and `b"noatodo/chacha-key/v1"` (chacha_key). `salt=None` is deliberate (the master secret is already a uniform Argon2id key; the salt lives in Argon2id, G16 header). Store only the salt and Argon2 params; **no passphrase/verification hash is stored** (that would be an offline oracle). Unlock is verified implicitly by the ChaCha20-Poly1305 tag: the wrong passphrase makes the AEAD decryption fail. Passphrase minimum length is 12 (N11.3). Never store the passphrase or derived keys.

**DPAPI pepper:** A random 32-byte pepper (second factor of the key derivation, gate G18) is stored in the Windows Credential Manager via `keyring`, never in the DB. This is the only use of `keyring` (there are no Microsoft tokens anymore). **No pepper recovery export (N11.3):** the vault is deliberately bound to this Windows account, so losing the account (or forgetting the passphrase) means the data is unrecoverable; the only escape is the lock-screen Reset (which wipes the vault).

**No cloud / no sync:** the app is purely local. There is no external service, no Microsoft integration, and nothing ever leaves the machine.

**Frontend fonts:** JetBrains Mono and Space Grotesk must be local `.woff2` files. No external font CDN, this is a local-first app.

**CSS:** The complete `<style>` section from `Planung/weiteres/NoaToDo UI Konzept.html` is extracted verbatim into `frontend/style.css`. Do not rebuild it from scratch. The design tokens (colors, spacing, fonts) are defined there and must not be reinvented.

**Completion sound:** Checking a task off plays a short "Datenstrom" blip (`playDoneSound` in `app.js`). It is synthesized live with the Web Audio API (square-wave oscillators, no audio file), specifically so it needs no `media-src` and stays compatible with the strict CSP (`default-src 'self'`). Do not replace it with a bundled `.mp3`/`.wav` (that would require loosening the CSP). The `AudioContext` is created lazily on the first check (browsers require a user gesture before audio) and reused. It is toggleable via the `sound` setting (default on, N11.6, built 2026-08-08): `playDoneSound()` returns early when `state.settings.sound === false`, and the settings modal has a "Sound" section for it. `Code/sound-preview.html` is a standalone dev scratch file for auditioning sound variants, not part of the app and not loaded by it.

## Security rules (mandatory: all untrusted input must follow these)

Task text and list names are user-entered free text and treated as **untrusted input** (the app is local, so there is no external data source, but the defensive rules cost nothing and stay mandatory). These rules apply everywhere such values touch code:

**Escape every foreign value before it reaches `innerHTML` (anti-XSS):** The frontend runs with full `pywebview.api.*` access, an XSS is effectively RCE against the backend. `app.js` renders by building HTML strings from template literals and assigning them to `root.innerHTML` (full re-render via `render()`); it does NOT use `textContent`/`createTextNode` per node. The mandatory rule is therefore: any (potentially) foreign value interpolated into one of those template strings (task text, list name, ids) MUST be wrapped in the `esc()` helper near the top of `app.js`, which escapes `& < > " '`.
```js
`<span class="t-text">${esc(t.text)}</span>`   // correct: foreign data wrapped in esc()
`<span class="t-text">${t.text}</span>`         // FORBIDDEN: unescaped interpolation
```
A bare `element.textContent = task.text` is also fine where a single node is set directly, but the prevailing pattern in this codebase is template literal + `esc()`. Never interpolate a value you did not escape. The CSP (below) is defense in depth, not a substitute for `esc()`.

**Content Security Policy:** `index.html` must have in `<head>`:
```html
<meta http-equiv="Content-Security-Policy"
      content="default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; img-src 'self' data:;">
```

**Parameterized SQL only:** All queries in `db.py` use `?` placeholders, no f-strings, no `.format()`, no string concatenation for values. (Exception: the whitelisted column name list in `edit_task`; see "Implementation details" above.)

**Input validation (gate G20, implemented 2026-07-17):** every bridge method carries a declarative schema at its `@bridge(schema={...})` decorator (validators `v_text`/`v_id`/`v_str_list`/`v_bool`/`v_task_fields` plus `SETTINGS_SCHEMA`/`_validate_setting` in `api.py`; introspectable via `method._schema` for the Phase 9 tests; violations raise `InvalidInput` -> catalog code `invalid`). The rules: truncate task text > 4096 chars, list names > 256 chars (no more `meta` after N11.1.3); strip control characters U+0000-U+001F (except newline/tab) from stored strings; `reorder`/`reorder_lists` require `ordered_ids` to be **exactly** the list's task set (open and done together) resp. the full list set (as a set: no missing, duplicate, foreign or cross-list id), else `invalid` and nothing is written (all-or-nothing, no partial reorder), then renumber `position` 0..n-1; `move_task` validates both ids (missing -> `not_found`, target = current list -> `invalid`), **keeps `done`** and appends the task to the end of its section in the target list (highest position; the frontend sorts each section by position), then renumbers source and target 0..n-1 (U11-Entscheid, Bauplan N11.2.2); and `set_setting` only accepts whitelisted keys (`accent`, `theme`, `density`, `sidebar`, `railPinned`, `sidebarWidth`, `sound`, `autoLock`, `exportDone`; `dark` and `toolbar` were removed from the whitelist with N11.6 on 2026-08-08 and now return `invalid`) **and validates the value per key (V5, 2026-07-15):** `theme`/`density`/`sidebar` against their enums, `accent` against the fixed six-value hex preset whitelist (the value lands in the DOM as a CSS variable, so the whitelist kills CSS injection via settings), `sidebarWidth` clamped to 180-520 on write (not only parsed on read), `sound` bool, `autoLock` integer from {0, 1, 5, 15, 30, 60}, `exportDone` bool; `edit_task.fields` is type-checked (known fields only, `text` string, `done` bool). Preferred shape: a declarative schema per bridge method at the `@bridge` decorator so Phase 9 can test the rules directly. Export filename hardening (G21) additionally replaces the Windows-forbidden characters `< > : " / \ | ? *` and `..` sequences with `_` and caps the name at about 120 chars before the reserved-device-name check (V6, applies to `export_list` and `export_all`).

## Lock policy (B.8)

The app is always fully locked or fully unlocked. Lock triggers (passphrase required on return):
- Lock button / `Ctrl+L`
- Panic (mouse only via the rail button; deliberately no keyboard shortcut, N5)
- App restart (always starts locked)
- Auto-lock after inactivity (configurable via the `autoLock` setting, default 15 min, `0` = never; N11.4). **"Inactivity" is defined in N11.4.2:** activity = input events in the app window's DOM (mouse/keyboard/wheel/scroll), NOT global system idle and NOT bridge calls in general. The frontend reports it throttled (leading edge, then at most every 30 s) via `activity_ping()`, which only stamps `last_activity` on the backend's monotonic clock. The backend timer is the **sole, fail-safe** authority: if pings stop (frontend hangs/crashes/XSS-killed), the app locks; the frontend can only defer the lock, never prevent it. **Only** `activity_ping` counts as activity (no other bridge call, so a background poll like `get_wifi_signal()` does not keep the app awake); it is NOT in `ALLOWED_WHEN_LOCKED`, so while locked it returns `locked` and does not touch the timer.

**No lock on:** window minimize, focus switch to another app, window resize/move, **Windows session lock (Win+L)**. Win+L does nothing for NoaToDo (N11.8.4): there is no `WTSRegisterSessionNotification` / `WM_WTSSESSION_CHANGE` / `WTS_SESSION_LOCK` hook and none may be added. The auto-lock is the only reliable lock; its background timer (monotonic clock, own thread) keeps running while the PC is locked, so the app is guaranteed locked when the user comes back after the timeout.

**Auto-lock while a native dialog is open (N11.11.5, U5 decision 2026-07-13):** the export save dialog and the onboarding folder dialog are the only modal native windows; tearing the main window down under one of them hangs or crashes the app. The fix is **not** "postpone the lock" (that would let anyone suppress the auto-lock forever by leaving `Ctrl+E`'s dialog open, threat class K3, and would let a returning dialog write a plaintext export while locked). The rule is to **split the teardown sequence**: on `autolock` with a dialog open, steps 1 to 7 of N11.11.2 run immediately (freeze the bridge, flush the write-back, clear the clipboard, close the DB, **zero the keys**) and the frontend renders the lock screen via `evaluate_js` (pure DOM, safe under a modal dialog); only the native steps 9 to 11 (tear down the view, wipe `PROFILE_DIR`) wait, and the sequence **closes the dialog itself** instead of waiting for it. A dialog returning after a lock has its result voided: no file is written, the export buffer is zeroed, the method returns `{"error": "locked"}`. Every other exit (lock button, `Ctrl+L`, panic, killswitch, reset, quit, window X) cancels the dialog at once and does not defer. All `create_file_dialog` calls go through one `_native_dialog()` context manager in `api.py` (flag released in `finally`, at most one dialog at a time, a second one returns the new `busy` error code); an open dialog is **not** activity and does not reset the auto-lock timer.

**Unlock, reset, rate-limit (Phase 8 target, N11.3/N11.4):** first run lets the user choose the vault file location (path stored in an unencrypted config, N11.3) and set a passphrase (min 12 chars, prominent "no recovery" warning). A forgotten passphrase has no recovery; the lock screen offers a **Reset** (killswitch-style confirm, then type `RESET`) that wipes the vault and restarts full onboarding (choose location + new passphrase). Passphrase is changeable in Settings. Wrong-passphrase rate-limit: 2 s after each attempt, 3 free tries, then an escalating lockout ladder 10 s / 30 s / 1 min / 5 min / 15 min / 30 min / 1 h / 5 h / 10 h (each stage allows 2 tries before advancing).

**Rate-limit state is persisted (N11.4.1, U6 decision 2026-07-13):** the ladder must survive a restart, otherwise the lock screen's own off button resets it in two clicks. `{fails, stage, next_try_at, locked_at, duration}` live in `config.json` (unencrypted, outside the vault, which is closed while locked; full schema in N11.15.1, Plananalyse U2 resolved). Only a successful `unlock()` clears it (and `reset_vault()`, which deletes everything anyway). **Persist before verify (pro-security):** a failed attempt is counted and `config.json` is written **atomically before** Argon2id/AEAD even run and before any response goes back, so killing the process mid-check can't dodge the count the way the off button did; every non-success outcome (wrong, crash, kill, power loss) leaves the raised state. Stage/`duration`/`next_try_at` derive from `fails` through **one** deterministic function (identical live and on restart); on a `fails`/`stage` mismatch the higher value wins. Two clocks, deliberately: `time.monotonic()` within a session (immune to clock tampering), UTC wall-clock timestamps across restarts; within a session the remaining lockout is `max(monotonic, wall-clock)`, never the shorter; if `now < locked_at` (clock turned back) or the values contradict each other, the current lockout **restarts in full**, never shortens. Honest scope (threat model K3): the ladder only slows the casual on-device guesser. Whoever can copy `tasks.db.enc` guesses offline, where no ladder exists (only Argon2id cost + DPAPI pepper stand there), and whoever has file access can delete `config.json` to reset the ladder. Never sell it as protection against K1.

**Onboarding and vault management (N11.13, U1 decision 2026-07-13):** boot is **three-valued**, not two: `get_boot_state()` returns `onboarding | locked | unlocked` and is the first and only call the frontend makes before rendering anything (the switch is N11.8.2: only the existence of `tasks.db.enc` at the path from `config.json` decides). `get_state()` stays two-valued on purpose so G13's "locked reveals nothing" rule stays sharp. Onboarding is a boot state, not a modal (no `Esc`, no way past creating the vault): three screens (location incl. cloud-path warning per G32, passphrase with the loss warning as mandatory text plus an active "I understand" checkbox, then done and unlocked with an empty list view). The loss warning must name **both** facts: forgotten passphrase = data gone, and the vault is bound to this Windows account (other PC or fresh profile = data gone even with the right passphrase). `config.json` schema (U2) and the passphrase-change details incl. the `.bak` generation (U8) are now both resolved (U2 in N11.15, 2026-07-15; U8 in N11.3 a-d): the change re-encrypts or securely deletes the `.bak` so nothing stays readable with the **old** passphrase, keeps the pepper, and lifts the Argon2 params to the G8 target (KDF-upgrade path), verified by a fixed Phase-9 crypto test.

**Shutdown/lock sequence (N11.11, gate G35, S5 decision 2026-07-13):** there is exactly **one** routine, `teardown(reason)` in `security.py`, and every exit runs through it (lock button, `Ctrl+L`, auto-lock, lock-screen off button, panic Finish, killswitch, reset, native window X, `atexit`). A second hand-written exit path is a gate violation. Order (N11.11.2, itself security-relevant): idempotency guard, resolve open native dialogs (every exit except auto-lock cancels the dialog outright; auto-lock still runs steps 1 to 7 immediately and defers only the native steps 9 to 11, see N11.11.5 above), freeze input (G13), **cancel the G17 debounce timer and persist pending changes synchronously** (skipped for killswitch/reset; on failure the sequence aborts into the N6 error screen rather than losing data; the G17 write-back debounces ~3 s after the last change but has a hard cap: **it writes at least every 30 s even under continuous edits**, so sustained typing cannot defer the write-back indefinitely, U20), **clear the clipboard if it still holds app content** (G23/V7), close the DB, zero the keys and discard the last-deleted-list undo buffer (G25, N11.2.1, so a locked app holds no deleted-task plaintext), only then delete files and the DPAPI pepper (killswitch/reset, U21), wipe `PROFILE_DIR` (G14; `LOCK_PROFILE_DIR` is never wiped), **restore the radio state last** (exit paths only, never on lock, N11.5/N11.10), release the mutex, exit. Steps after the flush are best effort: a failing step is skipped, never blocks the following ones.

## Bridge API (`pywebview.api.*`)

All frontend<->backend communication goes through these methods on the `Api` class in `backend/api.py`. Each returns a JSON-serializable value (or `{ "error": "code", "message": "..." }` on failure; the **canonical error-code table lives in Bauplan B.2**, see "Error hygiene" above, and a code that has no row there must not reach the frontend). **As of Phase 8 (2026-07-21) all the methods below are implemented for real** (the "(planned)"/"(stub)" notes in the rows are historical; the security methods run through the `teardown` sequence, unlock derives keys and opens the vault, onboarding creates the real vault). There is also `open_existing_vault(path)` (N11.15.6: point the config at an existing `tasks.db.enc` and go to the lock screen). The Api no longer holds a DB directly: it holds a `security.Session` with a `Vault`, and `self._db` (underscore, PyWebView introspection rule) is the unlocked DB.

| Method | Args | Returns |
|---|---|---|
| `get_boot_state()` | (keine) | `{ state: 'onboarding'\|'locked'\|'unlocked', vault_path, resumed }` (N11.13: the boot switch, first and only call before the frontend renders anything; allowed while locked. `resumed` (N11.22) is `false` on a real program start and `true` when the running session was locked in between and unlocked again; main.py's boot loop sets it, and the frontend uses it to skip the welcome screen after an unlock) |
| `get_state()` | (keine) | `{ lists, settings, online, locked, system_theme }` (only after unlock; locked it returns just `{ locked: true }`, G13). `system_theme` is `'dark'\|'light'\|'unknown'` (N11.6): the Windows state rides along so `theme=auto` is right on the first render, `'unknown'` means honestly unreadable |
| `get_lists()` | (keine) | `[{ id, name, open:[task], done:[task] }]` |
| `add_list(name)` | str | `{ id, name, ... }` |
| `rename_list(id, name)` | str, str | `{ ok }` |
| `delete_list(id)` | str | `{ ok }` (keeps the deleted list in the single RAM undo buffer, N11.2.1) |
| `undo_delete_list(id)` | str | `{ ok }` (restores the last deleted list at its old position, same ids; `not_found` if the buffer was replaced/discarded; buffer discarded on lock/panic/killswitch/quit) |
| `add_task(list_id, text)` | str, str | `{ ...task }` (no meta field, N11.1.3) |
| `toggle_task(id)` | str | `{ id, done }` |
| `edit_task(id, fields)` | str, obj | `{ ...task }` (text only, no meta, N11.1.3) |
| `delete_task(id)` | str | `{ ok }` |
| `reorder(list_id, ordered_ids)` | str, [str] | `{ ok }` (task order within a list) |
| `reorder_lists(ordered_ids)` | [str] | `{ ok }` (sidebar list order via drag and drop; N11.2.2 edge cases: exactly the full list set or `invalid`, implemented 2026-07-17) |
| `move_task(id, target_list_id)` | str, str | `{ ...task }` (move task to another list: drag onto a sidebar entry or right-click "Move to…" menu; keeps `done`, appends to the end of its section in the target, N11.2.2, implemented 2026-07-17) |
| `export_list(id, format)` | str, `'md'`\|`'txt'` | `{ ok, filename }` (JSON dropped, N11.1.5; shows the save dialog in the backend and really writes the file, G21c: UTF-8 no BOM, CRLF; dialog cancel -> `canceled` (silent), second dialog -> `busy`; suggested filename = G21/V6-sanitized list name) |
| `export_all(format)` | `'md'`\|`'txt'` | `{ ok, filename }` (all lists in one file, sidebar order, names verbatim incl. duplicates; suggested filename `NoaToDo-Export-YYYY-MM-DD.<format>`; same dialog path as `export_list`; implemented 2026-07-17) |
| `copy_task(id)` | str | `{ ok, clears_in }` (hardened backend clipboard copy of ONE task) |
| `copy_errors()` | (keine) | `{ ok, clears_in }` (copies the redacted G29 error ring buffer via the same hardened G23 clipboard path; status modal "Recent errors" copy button) |
| `set_setting(key, value)` | str, * | `{ ok }` |
| `set_mini(flag)` | bool | `{ mini }` |
| `get_status()` | (keine) | `{ db, encryption, bitlocker, runtime, app, errors }` (`app` = `{version, build_date, commit, frozen, signed, source}` from `buildinfo`, Phase 9/V10) |
| `set_online(flag)` | bool | `{ online, partial, access, refused }` (N11.5, real Windows airplane-mode toggle over the WinRT radio APIs, `backend/radio.py`): switches all managed radios, answers only after completion with the verified real state, offline aggregates security-first (`online:true` while any radio stays on), `partial:true` + `refused` name a disobedient radio; no toast, the honest pill is the message (N11.16); denied/missing access -> `access:'denied'`/`'unavailable'`, no radio touched |
| `get_wifi_signal()` | (keine) | `{ connected, percent, level }` (level 0-3, real WLAN strength via `netsh wlan show interfaces`; cosmetic, drives the rail WiFi icon; N11.5 polls every 10 s only while online + window visible + unlocked, paused otherwise, never counts as auto-lock activity, U15) |
| `activity_ping()` | (keine) | `{ ok }` (planned, N11.4.2: resets the auto-lock timer; frontend-throttled, leading edge then at most every 30 s; only stamps `last_activity` on the monotonic backend clock, takes no timestamp, cannot disable the timer; **not** in `ALLOWED_WHEN_LOCKED`, returns `locked` while locked; no other bridge call counts as activity) |
| `choose_vault_dir()` | (keine) | `{ path, has_vault, warning, bitlocker }` or `{ error: 'canceled' }` (N11.13: native folder dialog, checks writability, warns on cloud paths per G32; `has_vault:true` if the folder already holds a `tasks.db.enc` so onboarding offers "open this vault" instead of "create new" per N11.15.6; `bitlocker` = `{state:'protected'\|'off'\|'unknown', drive}`, the real status of the chosen drive, feeding the G31a recommendation on step 1; allowed while locked) |
| `create_vault(path, passphrase)` | str, str | `{ ok, legacy_removed }` (N11.13: pepper + salt + Argon2 params, empty DB, writes `tasks.db.enc` and the path into `config.json`; app is unlocked afterwards; **refuses with `invalid` if a `tasks.db.enc` already exists at `path`, an existing vault is never overwritten, N11.15.6**; `legacy_removed:true` means an old dev DB was really found and secure-deleted, which is the only trigger for the G33 one-time notice; allowed while locked) |
| `change_passphrase(old, new)` | str, str | `{ ok }` (planned, N11.13: Settings only, **not** in the locked allowlist; fresh salt + nonce, pepper stays; the `.bak` generation is re-encrypted with the new key (preferred, keeps the G16 crash backup) or removed via the secure-delete path so nothing stays readable with the old passphrase, and the Argon2 params are lifted to the G8 target = the KDF-upgrade path, N11.3 a-d, U8 resolved) |
| `reset_vault()` | (keine) | `{ ok }` (planned, N11.13: forgotten-passphrase exit; runs `teardown(reason='reset')`, deletes vault + `.bak` + metadata + pepper, then restarts onboarding; allowed while locked) |
| `lock()` | (keine) | `{ locked: true }` (frontend-only, gate G13 not enforced; the frontend clears the workspace and goes offline before calling it, N10) |
| `unlock(passphrase)` | str | today a placeholder returning `{ ok }`. **Phase 8 target (N6, resolves U7):** `{ ok:true }` on success, else the canonical B.2 codes, decided **before** the costly Argon2 by inspecting the unencrypted container header: missing file -> `vault` (never silent onboarding, that is only `get_boot_state()`), unreadable header (magic/version/salt/params/nonce) -> `vault` (N6 damaged screen + `.bak` offer), well-formed header but failing AEAD tag -> `passphrase` (+ `retry_in`), running lockout ladder -> `rate_limited` (+ `retry_in`). Only `passphrase` advances the N11.4 ladder; a `.bak` restore is a full unlock attempt under the same ladder and overwrites the primary only after it verifies; after `DAMAGE_HINT_AFTER = 5` consecutive `passphrase` results the lock screen adds a neutral "maybe damaged, try a backup?" hint. Frontend re-fetches `get_state()` after success |
| `panic()` | (keine) | `{ locked: true }` (frontend-only; flow ends in the end screen with Finish/Killswitch, N10) |
| `killswitch()` | (keine) | `{ ok }` (REALLY and irreversibly wipes all DB content, rewrites default settings + `seeded` marker; only reachable from the panic end screen) |
| `quit_app()` | (keine) | `{ ok }` (closes the app window via `BeginInvoke` on the UI thread; lock-screen off button, panic Finish, killswitch end) |

Backend -> frontend events (via `window.evaluate_js`): `window.noa.onLocked()`, `window.noa.onNetChange(online)` (N11.5: an external radio change was mirrored; the pill follows the OS state), `window.noa.onSystemTheme(dark)` (N11.6: Windows switched light/dark; the backend only sends it while `theme=auto`, and the handler sets `data-theme` via `applyChrome()` without a `render()`).

## SQLite schema (2 main tables + settings)

`lists(id, name, position, created_at, updated_at)`.  
`tasks(id, list_id, text, done, position, created_at, updated_at)` (the `meta` column was removed 2026-07-17 per N11.1.3, a task is only `text` + `done`; `_drop_legacy_columns()` migrates old dev DBs). **Position invariant (Bauplan B.1, U13 decision 2026-07-15, implemented 2026-07-17):** `position` is kept **per section**, i.e. `open` (`done=0`) and `done` (`done=1`) each have their own 0..n sequence within a list. Checking a task off appends it to the **end of `done`** (`MAX(position) + 1` among that list's done tasks); reopening appends it to the **end of `open`**; a new task appends to the end of `open` (`MAX+1` only among `done=0`); `reorder` takes the list's full task set (open + done, N11.2.2) and renumbers 0..n-1 per section; each section is ordered by `(position, created_at)`. The frontend cache mirrors this (checked task is push-ed to the end of `done`).  
`settings(key, value)`, current key/value pairs: `accent`, `theme`, `density`, `sidebar`, `railPinned`, `sidebarWidth`, `exportDone`, `sound`, `autoLock`. The former keys `dark` and `toolbar` are **gone** since N11.6 (2026-08-08): they are out of the whitelist and `db._migrate_settings()` deletes their rows from existing vaults while writing a one-time `theme = auto`. All values stored as strings; the bool-typed keys read back as bool via `_BOOL_SETTINGS` in `api.py` (`exportDone`, `sound`), `railPinned` is compared to the string `'true'`, `sidebarWidth` is parsed as int (valid range 180-520; **first-start default 300**, N11.19/2026-07-25: `_DEFAULT_SETTINGS` in `db.py` seeds `"300"` into a freshly created vault only, a stored user value is never touched; the frontend constant `SIDEBAR_WIDTH_DEFAULT` in `app.js` and the CSS fallback `var(--sidebar-width, 300px)` hold the same number and only apply when nothing plausible is stored). `autoLock` (minutes, default 15, `0` = never; {0,1,5,15,30,60}) drives the fail-safe auto-lock timer (B.8.3) and has a segment in the settings modal; `sound` (default on) has its own "Sound" section in the settings modal since 2026-08-08 and gates `playDoneSound()`. `theme` (`auto|light|dark`, default `auto`, N11.6) drives `data-theme` through `effectiveTheme()` in `app.js` and the native titlebar through `Api._effective_dark()`. `exportDone` (bool, default `true`, N11.2.4/U10 Punkt 6) decides whether completed tasks appear in exports (`export_list`/`export_all`, md and txt); the "Export" section of the settings modal toggles it, the `_export_include_done()` helper feeds `_export_md`/`_export_txt`. The rail is always `floating`, which is why `toolbar` could be dropped entirely. `seeded` (`"true"`) is a backend-only marker written on the first run (by `seed_if_empty()`) and by `killswitch()`; `seed_if_empty()` only writes the default settings and this marker, it never creates demo lists/tasks, so the app always starts with empty lists.

IDs: local items use `'l'+uuid` (lists) or `'t'+uuid` (tasks).

## Frontend state model

`app.js` holds a single in-memory cache object:
```js
state = {
  lists, activeId, settings, online, locked,
  modal,           // 'status' | 'rename' | 'shortcuts' | 'settings' | null
                   // (the former `menu`/'profile' state and the dead profile menu were removed 2026-07-17, Phase 7;
                   // the dead 'delete' confirm modal was removed the same day, Audit 1.2: task delete stays immediate)
  ctxList,         // right-click list context menu: { id, x, y } | null
  ctxTask,         // right-click task context menu "Move to…": { id, x, y } | null
  exportPill,      // two-step export pill: { step:'scope'|'format', scope } | null
  renamingId,      // list being renamed inline (sidebar pill) | null
  confirmDeleteId, // list whose inline delete confirmation pill is open | null
  listEditDock,    // inline rename/delete shown in the bottom dock instead of the sidebar
  focus,           // focus mode (hides sidebar + toolbar)
  mini,            // compact mini-window mode
  railPinned,      // right toolbar rail pinned (persisted as setting)
  sidebarWidth,    // sidebar width in px, first-start default 300 (persisted as setting)
  adding,          // new-list inline input visible
  addingTask,      // new-task inline input visible (bottom dock)
  doneOpen,        // completed section expanded
  editingId,       // task being edited inline (double-click) | null
  selectedId,      // task selected by click (target for rail copy/edit) | null
  panic,           // panic flow (N10): { armed, stage:'panel'|'wiping'|'done'|'killing', killArmed } | null
  onboarding,      // boot state (N11.13): { step, path, hasVault, warning, bitlocker, busy, error } | null
                   // step 1 location (+G32 warning, +G31a BitLocker note), 2 passphrase,
                   // 3 create/error, 4 only when legacy_removed: the G33 one-time notice
  welcome,         // welcome screen on every way into the unlocked app (N11.20): { leaving } | null
  systemTheme,     // Windows light/dark state (N11.6): 'dark'|'light'|'unknown';
                   // from get_state(), then via window.noa.onSystemTheme. Only
                   // matters while settings.theme === 'auto' (see effectiveTheme()).
}
```
`clearWorkspace()` resets the volatile part of this state (lists, active list, selection, menus, inputs, offline) and is called by both `doLock()` and the panic confirm; the lock/unlock cycle therefore always ends in a fresh `get_state()`.

The backend is the source of truth. After each mutating action: apply the backend response to state, then re-render the affected part. `railPinned` and `sidebarWidth` are not returned by `get_state()` directly; they are read back from `state.settings` during boot and written to settings via `set_setting()`.

**Rendering and event dispatch:** `render()` rebuilds the whole UI as an HTML string and assigns it to `root.innerHTML`; there is no per-node diffing. Interaction is handled by event delegation, not per-element listeners: clickable elements carry a `data-act="..."` attribute (often with `data-id`), and a central handler dispatches on `data-act`. Add new interactions by emitting a `data-act` in the template and adding a `case` to that dispatcher, not by attaching listeners after render (they would be lost on the next `render()`).

## UI layout

CSS Grid: `Header` (full width, 56px) over three columns: `Sidebar` (width via `--sidebar-width`, first-start default 300px, user-resizable by dragging the right edge) | `Main` (max 720px, centered) | `Toolbar` (right rail). Controlled by `data-theme`, `data-density`, `data-toolbar`, `data-sidebar`, `data-resizing` attributes on `.app`, plus `--accent` and `--sidebar-width` CSS variables. During sidebar resize, `data-resizing` is set on `.app` to suppress the width transition.

## Build phases (from `Planung/Bauplan - NoaToDo.md`)

Phase 0 (scaffold) -> Phase 1 (db.py) -> Phase 2 (api.py) -> Phase 3 (main.py wiring) -> Phase 4 (index.html skeleton) -> Phase 5 (style.css + fonts) -> Phase 6 (app.js full UI) <- *locally usable milestone* -> Phase 7 (export + Undo + move_task/reorder_lists) -> Phase 8 (lock/panic/encryption) -> Phase 9 (build/packaging, portable .exe). The former Microsoft login and Graph sync phases were removed on 2026-07-09, the notifications phase on 2026-07-09, and the later phases renumbered accordingly.

Complete acceptance criteria for each phase are in `Planung/Bauplan - NoaToDo.md`.

## Keyboard shortcuts (B.5)

Bauplan B.5 is the **single source of truth** for shortcuts (fully re-derived from the real `onKeyGlobal` on 2026-07-13, resolving Plananalyse W6). Whoever changes or adds a shortcut updates the code, Bauplan B.5, the shortcuts modal (`?`), and this table in the same change.

| Action | Key |
|---|---|
| New task | `Enter` (in new-task field) |
| New task in open list | `Ctrl+N` (toggles the bottom new-task field, second press closes it; needs an open list) |
| New list | `Ctrl+Shift+N` (toggles the new-list field, second press closes it) |
| Toggle sidebar | `Ctrl+B` |
| Focus mode | `F` (needs an open list; exit always works) |
| Switch list | `Ctrl+ArrowUp` / `Ctrl+ArrowDown` (sidebar open and a list open; stops at the ends, no wrap-around) |
| Open list 1-9 | `Ctrl+1` bis `Ctrl+9` (opens the n-th sidebar list, 1 = topmost; same key again toggles it closed; only when the sidebar is open, a list open with the sidebar closed does not count) |
| Lock app | `Ctrl+L` |
| Export | `Ctrl+E` (opens the two-step export pill: scope, then format, then the backend save dialog; second press or `Esc` closes it; with no open list the "current list" scope option is greyed out, only "all lists" is selectable, N11.2.3; no-op in mini mode) |
| Toggle theme | `Ctrl+J` (from `auto`: override to the opposite of the shown theme; from a fixed theme: the other fixed theme; back to `auto` only via the Settings segment) |
| Online/offline | `G` |
| Shortcut help | `?` |
| Close all | `Esc` (closes menus/modals/inputs, clears selection and focus mode; in mini mode it exits mini; closes the panic panel but never the running/finished wipe screen; also works while typing) |

Deliberately WITHOUT a shortcut (per B.5, do not add one): the panic flow (rail button only, two-stage arming; decided 2026-07-13, Bauplan N5, the former `Ctrl+Shift+!` idea is dropped for good), copy (rail button only, gate G23, no `Ctrl+C` app shortcut), and mini mode (rail button only; `Esc` exits it). Mouse gestures (click = select, double-click = inline edit, drag = reorder tasks; since Phase 7 also: drag a sidebar entry = reorder lists, drag a task onto a sidebar entry or right-click a card ("Move to…") = move task) are documented in the shortcuts modal.

Letter hotkeys (`F`, `G`, `?`) must not fire while focus is inside an input or textarea; the exceptions are `Esc` and the `Ctrl+N`/`Ctrl+Shift+N` toggles (so the second press can close the freshly opened field). Both call `e.preventDefault()` so the triggering letter does not land in the freshly focused input (and to suppress the browser's default new-window action). While the app is locked, all shortcuts are disabled; any printable key instead focuses the lock-screen password input (the character itself is then inserted normally).

## Key dependencies

`pywebview`, `sqlcipher3-wheels` (import `sqlcipher3`), `cryptography`, `argon2-cffi`, `keyring` (only for the DPAPI pepper, gate G18). See `Code/requirements.txt`.

**For the real airplane-mode toggle (N11.5, resolves U14; in `requirements.txt`/`requirements.lock.txt` as of 2026-07-21, `winrt-*==3.2.1`):** the modular PyWinRT packages `winrt-runtime`, `winrt-Windows.Devices.Radios`, `winrt-Windows.Devices.Enumeration`, `winrt-Windows.Foundation` and `winrt-Windows.Foundation.Collections` (smaller surface than the `winsdk` bundle, which stays only a fallback). The Collections package was **not** in the Bauplan's original list; it turned out to be required because the `IVectorView` returned by `GetRadiosAsync()` is not iterable without its projection (found and added when building N11.5). `set_online` enumerates radios via `Radio.GetRadiosAsync()`, switches each WiFi/Bluetooth/MobileBroadband radio with `SetStateAsync`, and reads the state back; there is no switchable airplane-mode flag in the public API, only individual radios. Denied `RequestAccessAsync` degrades visibly (tooltip "no radio access", no radio touched, state unchanged). All packages are pinned in `requirements.lock.txt` under gate G11.

**Python version is pinned to 3.11.x (gate G11, Plananalyse U25):** the interpreter is treated as a pinned dependency, not just the packages. `sqlcipher3-wheels` ships wheels only for specific CPython versions, and the Phase 9 `.exe` must be built against exactly this interpreter. Today's setup runs on Microsoft-Store Python 3.11; the release build (Phase 9) runs under 3.11.x as well. Do not build or target another Python minor version without updating G11 in the Bauplan first.
