[pytest]
# Aufruf aus src/:  .\venv\Scripts\python.exe -m pytest
# (tests/conftest.py haengt src/ an sys.path und biegt vorher alles weg, was
# echte Nutzerdaten waeren: LOCALAPPDATA, config.json, DPAPI-Pepper.)
testpaths = tests
addopts = -q
