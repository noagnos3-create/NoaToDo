# NoaToDo: Werkzeuge fuer Entwicklung und Auslieferung (Bauplan Phase 9).
# Gehoeren NICHT in die App und NICHT ins Bundle (die Spec schliesst pytest und
# PyInstaller ausdruecklich aus). Installieren mit:
#   .\venv\Scripts\python.exe -m pip install -r requirements-dev.txt

# Testbasis (Phase 9, Punkt 1)
pytest==8.4.2

# Build der auslieferbaren One-file-.exe (Entscheid N11.29 a)
pyinstaller==6.16.0

# Nur fuer tools/make_icon.py, das aus assets/noatodo-logo.png die
# frontend/icon.ico erzeugt. Die App importiert selbst kein PIL; deshalb steht
# Pillow hier und nicht mehr in requirements.lock*.txt (dort war es nur, weil
# die Lock-Dateien aus einem pip freeze der Entwicklungsumgebung stammen).
pillow==12.2.0
